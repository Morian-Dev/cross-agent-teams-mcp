---
status: warn
change: refactor-mailbox-routing
generated_at: 2026-04-19T00:00:00Z
stages:
  spec_compliance: { status: pass, issue_count: 1 }
  code_quality:    { status: warn, issue_count: 12 }
  integration:     { status: warn, issue_count: 5 }
issue_count: 18
warn_issues:
  - stage: code_quality
    id: CQ-01
    tag: dead-param
    file: src/mcp/broadcast.ts
    line: 38
    summary: "BroadcastService constructor parameter `_send: SendMessageService` is unused after runFanoutWithRetry extraction; drop it and update tools.ts:108 wiring."
  - stage: code_quality
    id: CQ-02
    tag: inconsistent-write-path
    file: src/mcp/broadcast.ts
    line: 77
    summary: "broadcast.ts writes events via raw INSERT while broadcast-to-role.ts uses EventsOutbox.append — fanout-with-retry dedups poke side but not write side; unify on EventsOutbox or factor a shared write helper."
  - stage: code_quality
    id: CQ-03
    tag: error-conflation
    file: src/mcp/broadcast.ts
    line: 46
    summary: "Returns `unknown_recipient` when caller is the only online agent — conflates empty-online-set with missing sender. Consider distinct `no_online_recipients` error (both broadcast and broadcast_to_role have this shape)."
  - stage: code_quality
    id: CQ-04
    tag: redundant-query
    file: src/mcp/get-inbox.ts
    line: 31
    summary: "Extra `SELECT role FROM agents WHERE agent_id=?` after `agents.findById` already returned the row (agents-repo already projects role). Drop the second lookup."
  - stage: code_quality
    id: CQ-05
    tag: magic-sentinel
    file: src/mcp/get-inbox.ts
    line: 45
    summary: "Uses sentinel string '__none__' as the null-role match to never-match. Brittle; omit the to_role OR-clause when callerRole is null instead."
  - stage: code_quality
    id: CQ-06
    tag: duplication
    file: src/mcp/send-message.ts
    line: 83
    summary: "lookupAgentFn SQL/shape duplicated between SendMessageService.send inline fanout wiring (lines 73-98) and fanout-with-retry.ts:34. send-message could call runFanoutWithRetry to eliminate the duplication."
  - stage: code_quality
    id: CQ-07
    tag: duplication
    file: src/mcp/broadcast.ts
    line: 89
    summary: "Message id generation `i === 0 ? baseId : ${baseId}-${i}` duplicated between broadcast.ts and broadcast-to-role.ts:98; extract fanoutMessageId helper."
  - stage: code_quality
    id: CQ-08
    tag: fresh-boot-assumption
    file: src/storage/schema.ts
    line: 1
    summary: "Schema switches team→from_team/to_team with no migration; any existing DB breaks on boot. Acceptable per project memory (skip-legacy-db-migration) and documented in proposal; flagging for visibility."
  - stage: code_quality
    id: CQ-09
    tag: test-name-mismatch
    file: tests/send-message-zod-schema.test.ts
    line: 51
    summary: "'accepts to_team as optional string' asserts service-level rejection (unknown_agent/unknown_recipient), not pure schema validation; name oversells it. Happy path is covered in cross-team test; safe to rename."
  - stage: code_quality
    id: CQ-10
    tag: loose-regex
    file: tests/broadcast-to-role-tool-registration.test.ts
    line: 37
    summary: "Regex `/to_team|unknown|unrecognized|validation/i` could match 'unknown_agent' instead of the strict-schema rejection. Tighten to `/unrecognized|strict|to_team/i`."
  - stage: code_quality
    id: CQ-11
    tag: api-surface
    file: src/mcp/fanout-with-retry.ts
    line: 14
    summary: "runFanoutWithRetry args object has 8 fields. Consider splitting into FanoutContext (db/team/from/deps) vs per-call (recipients/body/messageId/sentAt)."
  - stage: code_quality
    id: CQ-12
    tag: skip-reason-semantics
    file: src/mcp/auto-poke-fanout.ts
    line: 62
    summary: "`!pokeFn` maps to reason 'tmux_unavailable' semantically it's missing-dep, not tmux failure; use distinct reason or document collapse."
  - stage: integration
    id: INT-01
    tag: synthetic-red
    file: openspec/changes/refactor-mailbox-routing/tasks.md
    line: 971
    summary: "Task 4.1 RED was forced via temporary 'wrong','wrong' mutation in broadcast.ts because 1.2's REFACTOR already landed the real behavior; legitimate regression-guard pattern but notes that RED did not exercise a fresh spec gap."
  - stage: integration
    id: INT-02
    tag: fragile-timing
    file: tests/send-message-cross-team-auto-poke.test.ts
    line: 1
    summary: "Retry test advances exactly 30_500ms for RETRY_DELAYS_S[0]; derive from the imported constant to avoid breakage if the backoff is tuned."
  - stage: spec_compliance
    id: SC-01
    tag: matrix-drift
    file: openspec/changes/refactor-mailbox-routing/tasks.md
    line: 1523
    summary: "Coverage matrix points 'Shutdown clears pending retry timers' and 'Invalid POKE_QUIET_MS env falls back to default' at send-message-auto-poke.test.ts but they actually live in poke-retry-shutdown.test.ts and poke-guard.test.ts:33. Scenarios ARE covered; only the file pointer is stale."
  - stage: integration
    id: INT-03
    tag: design-cross-link
    file: openspec/changes/refactor-mailbox-routing/design.md
    line: 108
    summary: "§D5 rationale 'for future cross-team events' has an executable anchor in tests/sse-fanout.test.ts:74-96; add a one-liner reference in design.md to close the loop."
  - stage: integration
    id: INT-04
    tag: timing-heuristic
    file: tests/broadcast-to-role.test.ts
    line: 94
    summary: "Parallel fan-out asserts elapsed < 400ms with POKE_QUIET_MS=50; CI-flaky on shared runners. Bound with a larger envelope (~800ms) or use a concurrency-counter."
  - stage: integration
    id: INT-05
    tag: design-cross-link
    file: openspec/changes/refactor-mailbox-routing/design.md
    line: 133
    summary: "§D8 should explicitly note display_name fallback to agent_id[:8] (currently only in Risks R7); send-message-cross-team-auto-poke.test.ts:82-99 doubles as R7 regression."
---

## Code Review: refactor-mailbox-routing

### Summary

| Stage              | Status                |
|--------------------|----------------------|
| Spec Compliance   | PASS — 1 suggestion   |
| Code Quality      | WARN — 7 warnings, 5 suggestions |
| Integration Review| WARN — 2 warnings, 3 suggestions |

No CRITICAL findings. Change is functionally complete; warnings point at cleanups and hardening opportunities.

### Stage 1: Spec Compliance

- Events-outbox capability: 5/5 requirements mapped, 10/10 scenarios covered (`src/storage/schema.ts`, `src/storage/events-outbox.ts`, `src/daemon/cleanup.ts`).
- Mailbox capability: 11/11 requirements mapped, 47/47 scenarios covered across the new/updated MCP tools.
- Counts: **16/16 requirements mapped, 57/57 scenarios covered.**

Findings:
- **SUGGESTION** (SC-01): Coverage matrix at tasks.md lines 1477–1539 points at `tests/send-message-auto-poke.test.ts` for two scenarios that actually live elsewhere (`tests/poke-retry-shutdown.test.ts` and `tests/poke-guard.test.ts:33`). Scenarios ARE covered; only the pointer is stale.

### Stage 2: Code Quality

Production files reviewed: 15. Test files reviewed: 8.

**WARNINGS (7):**
- CQ-01 `src/mcp/broadcast.ts:38` — dead `_send` parameter after fanout-with-retry extraction; drop + update tools.ts:108.
- CQ-02 `src/mcp/broadcast.ts:77` vs `src/mcp/broadcast-to-role.ts:84` — inconsistent write path (raw INSERT vs EventsOutbox.append). Partial-extraction of fanout-with-retry leaves write-side duplication.
- CQ-03 `src/mcp/broadcast.ts:46` — error `unknown_recipient` when caller is the only online agent conflates empty set with missing sender; consider `no_online_recipients`.
- CQ-04 `src/mcp/get-inbox.ts:31` — redundant `SELECT role` after `agents.findById` already projected role.
- CQ-05 `src/mcp/get-inbox.ts:45` — magic sentinel `'__none__'` for null-role matching; build SQL conditionally instead.
- CQ-06 `src/mcp/send-message.ts:83` — inline fanout wiring duplicates `runFanoutWithRetry` logic; could call the helper directly.
- CQ-07 `src/mcp/broadcast.ts:89` — `fanoutMessageId` inline in both services; extract.
- CQ-08 `src/storage/schema.ts` — fresh-boot assumption documented; flagging for visibility.

**SUGGESTIONS (5):**
- CQ-09/10 test naming + regex tightening.
- CQ-11 fanout-with-retry args object split.
- CQ-12 skip-reason semantics for missing pokeFn.
- All 14 tasks using `kind: unit-test` is appropriate for this scope (no real-device or build-only criteria).

### Stage 3: Integration Review

- Bridge types detected: MCP HTTP server (fastify + StreamableHTTPClient), SQLite storage, mocked tmux capture-pane/send-keys, setTimeout-based retry scheduler + fake timers.
- Forwarding-only tests: **0 found, 0 as sole coverage.**
- Observed output blocks: **42 blocks reviewed, 0 placeholders, 0 fraud.** RED≠GREEN byte-distinct across all tasks.
- Runtime Assumptions cross-checked:
  - A1 (NOT NULL on from_team/to_team) → verified by `tests/schema-from-to-team.test.ts:55-61`. **PASS**
  - A2 (agent_id globally unique) → verified by `tests/send-message-cross-team.test.ts:26-62`. **PASS**
- Missing assumptions: **none** (design.md §Runtime Assumptions paragraph explicitly enumerates "默认/保持不变/与 X 一致" phrases and maps them to in-repo code).
- Full-suite baseline: **240 passed / 0 failed** (75 files, 10.54s).

**WARNINGS (2):**
- INT-01 Task 4.1 RED via synthetic `'wrong','wrong'` mutation (disclosed in tasks.md). Legitimate regression-guard since 1.2 already landed real behavior; noted for reviewer awareness.
- INT-02 `tests/send-message-cross-team-auto-poke.test.ts` advances hardcoded 30_500ms; derive from `RETRY_DELAYS_S[0]`.

**SUGGESTIONS (3):**
- INT-03/INT-05 design.md cross-links for §D5 (fanout to_team) and §D8 (display_name fallback).
- INT-04 `tests/broadcast-to-role.test.ts:94` parallel-fanout timing heuristic may be CI-flaky.

### Findings by Severity

**CRITICAL:** none

**WARNING (9):**
- CQ-01 broadcast.ts:38 dead `_send` param
- CQ-02 broadcast.ts:77 inconsistent event write path
- CQ-03 broadcast.ts:46 error conflation
- CQ-04 get-inbox.ts:31 redundant role query
- CQ-05 get-inbox.ts:45 `__none__` sentinel
- CQ-06 send-message.ts:83 duplicate fanout wiring
- CQ-07 broadcast.ts:89 fanoutMessageId duplication
- CQ-08 schema.ts fresh-boot (visibility only)
- INT-01 tasks.md:971 synthetic RED for 4.1
- INT-02 send-message-cross-team-auto-poke.test.ts hardcoded 30_500ms

**SUGGESTION (9):**
- SC-01 matrix drift for 2 scenarios
- CQ-09/10/11/12 test names, regex, args shape, skip reason
- INT-03/04/05 design cross-links, timing heuristic

### Verdict

No critical issues. 9 warnings worth addressing (mostly code-cleanup around the partial fanout-with-retry extraction; they do not block spec compliance or runtime correctness). Recommend:

1. A single follow-up commit fixing CQ-01 (dead param) and CQ-02/06/07 (complete the fanout extraction — unify write path + reuse helper in send-message) before archive. These four cluster around "fanout-with-retry was a half-extraction."
2. CQ-04/05 get-inbox cleanup in the same commit or a separate `refactor(get-inbox)`.
3. SC-01 matrix drift fixed in-place in tasks.md (non-blocking).
4. INT-02 / CQ-09/10 test tightening can ship with the same commit as the cleanup.

Run `/ts-verify` once the above are addressed.

STATUS: warn
