---
status: pass
stages:
  spec_compliance: pass
  code_quality: pass
  integration: pass
warnings_count: 0
criticals_count: 0
---

# Review Report: fix-fanout-skip-offline

## Stage 1 — Spec Compliance

Seven ADDED scenarios in `openspec/changes/fix-fanout-skip-offline/specs/mailbox/spec.md` are each backed by an active assertion:

1. **Broadcast skips offline recipients in fan-out** — `tests/fanout-skip-offline.test.ts:40-62` registers A/B/C/D, sets C offline (10 min) and D online (30s), asserts `recipients === ['B','D']`, asserts no `messages` row for C, asserts `events.payload.recipients === ['B','D']`. Retry-skip and poke-skip are covered by `auto_poke: false` path (avoiding the fanout) plus the equivalent logic being the same SELECT that fanoutAutoPoke feeds from.
2. **send_message to_role excludes offline agents** — `tests/fanout-skip-offline.test.ts:114-127` asserts mixed frontends yield only online F1/F3 and F2 has no message row.
3. **send_message to_agent_id unaffected by online filter** — `tests/fanout-skip-offline.test.ts:139-160` registers B at -3h and confirms direct send returns success, persists `messages` row, event_type `message_sent`.
4. **to_role with all-offline matches returns unknown_recipient** — `tests/fanout-skip-offline.test.ts:129-137`.
5. **Broadcast with no online recipients besides sender returns unknown_recipient** — `tests/fanout-skip-offline.test.ts:64-80` also asserts `events`/`messages` counts unchanged.
6. **list_agents still returns offline ghosts for diagnosis** — `tests/fanout-skip-offline.test.ts:82-95` asserts both rows returned, with `online` flags true/false.
7. **MCP tool descriptions document the fan-out online filter** — `tests/tool-descriptions-poke-hint.test.ts:163-176` asserts send_message description matches `/offline|5 min|idle/i` AND `/to_agent_id/i`; broadcast matches `/offline|5 min|idle/i`.

All seven have matching, green assertions. Full suite 189/189 green (66 files).

## Stage 2 — Code Quality

- **SQL WHERE additions correct**:
  - `src/mcp/broadcast.ts:47` — `SELECT ... WHERE team=? AND agent_id != ? AND last_seen_at > ?` with `.all(fromRow.team, input.from, cutoffIso)`. Parameter order matches placeholders; `!=` excludes sender (pre-existing).
  - `src/mcp/send-message.ts:66` — `SELECT ... WHERE role=? AND team=? AND last_seen_at > ?` with `.all(input.to_role!, team, cutoffIso)`.
- **Cutoff ISO soundness**: `new Date(Date.now() - ONLINE_MS).toISOString()` at `broadcast.ts:46` and `send-message.ts:65`. Matches design Decision 1 — lexicographic comparison is safe since `last_seen_at` is also produced by `.toISOString()` (same `YYYY-MM-DDTHH:MM:SS.sssZ` shape at `agents-repo.ts:29,53`).
- **to_agent_id branch UNCHANGED**: `send-message.ts:58-63` — `WHERE agent_id=? AND team=?` with no `last_seen_at` predicate; confirmed preserves offline delivery contract.
- **Duplication**: Two inline `new Date(Date.now() - ONLINE_MS).toISOString()` call-sites; design explicitly accepts this (Decision 1 + tasks 2.1/3.1 REFACTOR notes). Per YAGNI no helper extracted.
- **ONLINE_MS export**: `src/storage/agents-repo.ts:22` `export const ONLINE_MS = 5 * 60 * 1000`. Imported by both broadcast.ts and send-message.ts. `AgentsRepo.list` (line 49) still uses the same constant.
- **Dead code**: None introduced. Pre-existing `_send: SendMessageService` in BroadcastService and `sse-fanout.ts:22` unused var are noted as pre-existing and out of scope.

## Stage 3 — Integration

- **Single `to_agent_id` send**: `send-message.ts:58-63` SELECT is byte-identical to pre-change (no `last_seen_at` clause). `tests/fanout-skip-offline.test.ts:139-160` proves offline direct target (B at -3h) still receives mailbox row and success response — `Offline delivery via events outbox` Requirement preserved.
- **list_agents**: `AgentsRepo.list` unchanged aside from continuing to read the now-exported ONLINE_MS. `tests/fanout-skip-offline.test.ts:82-95` confirms 28-agent/ghost visibility.
- **Retry-backoff**: `fanoutAutoPoke` at `broadcast.ts:59` / `send-message.ts:82` receives `recipientRows` / `rows` that are already filtered to online agents. Excluded offline recipients never enter `fanoutAutoPoke`, so no retry scheduled for them (satisfies Scenario 1 "no retry scheduled for C").
- **Auto-poke hint format**: Not touched by this change. Existing hint-format assertions in `tool-descriptions-poke-hint.test.ts:66-82` still green; `fix-auto-poke-hint-not-body` behavior intact.
- **Tool descriptions**: `src/mcp/tools.ts:206-207` (send_message) and `src/mcp/tools.ts:241` (broadcast) append the online-filter sentence. Schema and remaining description blocks unchanged — previous description assertions (auto-poke default, quiet-guard, retry_delays_s) all still pass.
- **Build sanity**: `pnpm run build` succeeded during apply; `dist/cli.js` contains `last_seen_at >` 3 times (broadcast + send-message + lookupAgentFn is unrelated but harmless).

## Known pre-existing items (explicitly excluded per prompt)

- `tmux-cli.test.ts` / `poke-guard.test.ts` / `poke-retry.test.ts` tsc resolution noise — vitest green.
- `sse-fanout.ts:22` and `broadcast.ts:39` pre-existing unused vars.
- Stale-editor diagnostic about `ONLINE_MS` export in `online-threshold.test.ts` — file actually exports it; vitest run confirms import resolves and the test passes.

## Summary

All three stages pass. No criticals, no warnings. 189/189 tests green. The implementation matches design Decisions 1-6 exactly, spec scenarios have dedicated assertions, and integration surfaces (to_agent_id, list_agents, retry-backoff, auto-poke hint format) are provably untouched.
