---
status: pass
change: fix-brand-sweep-self-match
generated_at: 2026-04-20T12:04:30Z
stages:
  spec_compliance: { status: pass }
  code_quality: { status: pass }
  integration: { status: pass }
critical_count: 0
critical_issues: []
---

# Three-stage review: fix-brand-sweep-self-match

## Stage 1 — Spec compliance: PASS

Delta spec at `openspec/changes/fix-brand-sweep-self-match/specs/daemon-core/spec.md` declares three Scenarios under the MODIFIED Requirement "Daemon source tree free of legacy brand word". Evidence mapping:

- Scenario `Brand-sweep grep returns zero matches` — enforced by `tests/brand-sweep.test.ts` (full-suite green; runtime-verified grep returns empty).
- Scenario `Main daemon-core spec file is literal-free` — enforced by sub-case 1 of `tests/brand-sweep-spec-self-check.test.ts` (reads spec file, asserts absence of `LEGACY_BRAND`). Runtime: `grep -c` returns `0` on the main spec file.
- Scenario `Allowlist covers the main daemon-core spec file as defense in depth` — enforced by sub-case 2 of `tests/brand-sweep-spec-self-check.test.ts` (parses `tests/brand-sweep.test.ts` for `--exclude-dir=daemon-core`).

Delta spec itself is literal-free (verified `grep -c` → 0). All three scenarios map 1:1 to test assertions. Scenario Coverage Matrix in `tasks.md` reports 3/3 and matches the delta.

## Stage 2 — Code quality: PASS

`git show` inspected for eb94b31, fa936cd, e505427.

- **eb94b31**: `openspec/specs/daemon-core/spec.md` +4/-2 (prose rewrite with placeholder + lookup block) and new `tests/brand-sweep-spec-self-check.test.ts` (+20). Minimal, surgical. No hard-coded brand literal in new source (`LEGACY_BRAND` built at runtime via `.join('-')`, mirroring `brand-sweep.test.ts`). Identifier names are clear. No commented-out code; the spec's inline parenthetical about the placeholder convention is intentional explanatory prose, not dead code.
- **fa936cd**: `tests/brand-sweep.test.ts` +8/-3 (adds `--exclude-dir=daemon-core` to `excludeArgs` with inline rationale comment) and self-check extension (+16). Inline comment is an intent comment per project rules, not explanatory padding. No literal brand in source. Names (`hasDirExclude`, `hasPathAllowlist`) are readable.
- **e505427**: `tasks.md` only — evidence-filling for Task 3.1. No source touched.

Diff is strictly minimal relative to stated goal. No `src/` or `plugins/` edits; exact match with Non-Goals NG3/NG4.

## Stage 3 — Integration: PASS

Full suite: **315/315 passing** (verified locally; matches tasks.md evidence and baseline delta of -1 failure, +2 new tripwire sub-cases, 0 regressions).

Allowlist evolution judgement: the pre-existing 4 entries in `ANTI_BRAND_ASSERTION_EXCLUDES` are all test files that assert the brand is absent elsewhere (negative-assertion pattern). Adding the daemon-core spec directory via `--exclude-dir=daemon-core` extends this precedent to a spec file — structurally analogous, as that file's MODIFIED Requirement IS the negative assertion. The `--exclude-dir` approach (vs `--exclude=spec.md`, which would silence all specs) is the correct fix for GNU grep's basename-matching semantics (design doc R2 documents this trade-off explicitly, with the scope-tightening follow-up commitment should `openspec/specs/daemon-core/` gain additional files). The precedent remains clean because (a) only one file exists under that directory, (b) the dedicated tripwire at `tests/brand-sweep-spec-self-check.test.ts` catches any future literal-prose regression inside the excluded scope. Defense-in-depth layering is symmetric: D1 (rewrite) is the primary guard, D2 (allowlist) is the backstop, D4 (tripwire) is the independent canary.

No new regressions, the prior failing sweep test is now green, and `openspec validate --strict` per Integration Readiness Checklist in tasks.md is marked passing.

STATUS: pass
