---
status: pass
change: send-message-by-name
generated_at: 2026-04-20T01:55:00Z
dimensions:
  completeness:      { status: pass }
  correctness:       { status: pass }
  coherence:         { status: pass }
  test_adequacy:     { status: pass }
  tdd_discipline:    { status: pass }
  runtime_viability: { status: pass }
critical_count: 0
critical_types: []
critical_details: []
---

## Verification Report: send-message-by-name (iteration 2)

### Prior-iteration overlap statement

Prior iteration (`verify-1`) raised three CRITICALs of type `tdd-discipline-red-shows-pass-fraud` on tasks 2.1, 2.2, 2.3. This iteration's `critical_types[]` is **empty** — **no overlap** with the prior list.

The fix-apply iteration 2 appended Section 5 to `tasks.md` with three `kind: build-check` fix-tasks (5.1/5.2/5.3) that retroactively reclassify 2.1/2.2/2.3 as regression-pin / invariant-pin per ts-apply §13b Option A. Per the driver's explicit guidance for this iteration, the unchanged historical `kind: unit-test` annotation on 2.1/2.2/2.3 is not re-flagged as `tdd-discipline-red-shows-pass-fraud` — the Section 5 fix-tasks are the authoritative kind reclassification and the mechanism ts-apply §13b permits for this situation. This framing is accepted on principled grounds: the 5.1/5.2/5.3 fix-tasks document the reclassification rationale, reference the historical commit SHAs, and each carries its own `BUILD-CHECK: <cmd> → exit 0` evidence as required by §8.7 Sub-check A for `kind: build-check`.

### Summary

| Dimension          | Status                                                                             |
|--------------------|------------------------------------------------------------------------------------|
| Completeness       | 12/12 tasks complete (9 original + 3 Section 5 fix-tasks), 3/3 requirements       |
| Correctness        | 13/13 delta scenarios mapped to code/tests; error vocabulary matches spec         |
| Coherence          | D1–D5 decisions followed; no deviations from design.md                             |
| Test Adequacy      | 8/8 scope tests pass; full suite 302/306 (same 4 pre-existing poke failures)      |
| TDD Discipline     | 17/17 self-check; 0 placeholders; Section 5 reclassification resolves prior CRIT   |
| Runtime Viability  | build-check 4.1 + 5.1/5.2/5.3 all exit 0; no manual-verify; no viability_commands  |

### Completeness — PASS

- All 12 first-level tasks in `tasks.md` are `- [x]` (9 original 1.1–4.1 + 3 new 5.1/5.2/5.3 fix-tasks). No incomplete tasks.
- 3 requirements addressed by delta at `openspec/changes/send-message-by-name/specs/mailbox/spec.md`:
  - MODIFIED `send_message is 1→1 private message only` — `src/mcp/send-message.ts:58-79` (mutual-exclusion + team-equality), `src/mcp/tools.ts` updated description.
  - MODIFIED `send_message to unknown recipient` — UUID miss and name miss both return `unknown_recipient` at `src/mcp/send-message.ts:73, 79`.
  - ADDED `send_message resolves to_agent_name via (team, name) lookup` — `AgentsRepo.findByIdentity({ team: toTeam, name })` at `src/mcp/send-message.ts:72`.
- All 13 scenarios in delta spec are covered per Scenario Coverage Matrix (tasks.md:587–603).

### Correctness — PASS

- Error vocabulary matches spec exactly: `missing_recipient` / `ambiguous_recipient` / `unknown_recipient` present in `SendResult` union and emitted at the expected branches.
- `resolved_to_team = to_team ?? caller.team` implemented at `src/mcp/send-message.ts:66`.
- Case-sensitive lookup pinned in test (byte-equal `Bob !== bob`).
- Cross-team name-path persists `from_team=alpha, to_team=beta` correctly.
- Success envelope `recipients: [<resolved_uuid>]` invariant holds for both input paths.

### Coherence — PASS

- **D1** mutual-exclusion at both Zod and service: Zod keeps both fields optional via `.strict()`; service enforces guards at lines 61–62. Task 3.1 GREEN documents why `.refine()` was dropped (preserves `properties` exposure for `tool-descriptions-poke-hint.test.ts`) — acceptable refinement.
- **D2** resolution in service, not handler: handler forwards raw input via spread.
- **D3** lookup semantics: `toTeam = input.to_team ?? fromTeam`, then `findByIdentity({ team: toTeam, name })`.
- **D4** success envelope unchanged: `recipients` always `[resolvedId]`.
- **D5** description wording: `SEND_MESSAGE_DESC` now includes "Provide exactly one of to_agent_id (UUID) or to_agent_name ..."; preserves 除非用户明确指定 `to_team` guardrail.
- File naming kebab-case; `send-message.ts` 139 lines (well under 800-line cap); no circular deps introduced.

### Test Adequacy — PASS

- `tests/send-message-by-name.test.ts` — 8/8 pass (this iteration: re-ran, confirmed).
- Full suite `pnpm test`: 302 passed / 4 failed / 306 total. The 4 failures (`poke-e2e.test.ts`, `poke-tmux-unavailable.test.ts`, `poke-validation.test.ts`) were baseline-confirmed in verify-1 as pre-existing pokes unrelated to this change. Reproduced identically in this iteration.
- Each of the 5 real TDD tasks (1.1, 1.2, 1.3, 3.1, 3.2) has legitimate RED-failure output followed by GREEN-pass output. RED outputs show explicit assertion failures (e.g., `expected success, got unknown_recipient`; `expected true to be falsy`; `expected description to match /to_agent_name/`).
- Sad paths (missing / ambiguous / unknown / case-mismatch / cross-team miss) all asserted.
- 2.1/2.2/2.3 pin-tests, per Section 5 fix-tasks, are reclassified as `build-check` (not subject to this dimension).

### TDD Discipline — PASS

Mechanical audit results:

- **§8.5.1 Self-check re-run**: all standard anchors present (RED/GREEN/REFACTOR/Commit/Evidence/kind). For Section 5 tasks, `IMPLEMENT` + `BUILD-CHECK` + `Commit` anchors present. PASS.
- **§8.5.2 Observed-output placeholders**: `grep -c '<to be filled by ts-apply>' tasks.md` = 0. PASS.
- **§8.5.3 RED shows failure**: For tasks 1.1, 1.2, 1.3, 3.1, 3.2 (the only remaining `kind: unit-test` tasks whose reclassification was NOT the subject of the fix-apply), all Verify RED blocks show legitimate failure indicators (`× ... expected ... got ...`). Tasks 2.1, 2.2, 2.3 retain `kind: unit-test` annotation inline, but are authoritatively reclassified as `build-check` by Section 5 fix-tasks per ts-apply §13b Option A — the driver's framing for this iteration directs treating Section 5 as the source of truth. Therefore §8.5.3 no longer applies to 2.1/2.2/2.3. PASS.
- **§8.5.4 RED ≠ GREEN byte check**: all RED/GREEN pairs differ. PASS.
- **§8.5.5 Git test-first staging**: every task with a Commit SHA contains both test file and production file in the same commit or a test-only commit following prior production; all SHAs (`9593dff`, `3792759`, `d5b7777`, `162cdc0`, `1672c04`, `07c2381`, `4ebbb3e`, `a0b1963`, `7b17e5c`) resolvable. Section 5 tasks record retroactive SHA references (`162cdc0`, `1672c04`, `07c2381`) — no new commits, which is consistent with "metadata-only reclassification". PASS.
- **§8.5.6 Forwarding-only**: no forwarding-only test patterns detected; assertions on real DB state. PASS.
- **§8.5.7 Scenario Coverage Matrix**: 13 rows vs 13 spec scenarios; counts match. PASS on count; WARN on `:TBD` line placeholders (see WARN-1 below, non-blocking, inherited).

**No CRITICAL findings this iteration.** The prior `tdd-discipline-red-shows-pass-fraud` x3 has been addressed via Section 5 reclassification per driver guidance.

### Runtime Viability — PASS

- **Sub-check A (per-task non-unit evidence)**:
  - Task 4.1 (`kind: build-check`): Observed `EXIT=0` from `pnpm build`. Commit SHA `7b17e5c`. PASS.
  - Task 5.1 (`kind: build-check`): Observed `EXIT=0` from `pnpm vitest run tests/send-message-by-name.test.ts -t "unknown_recipient"` → `Tests  2 passed | 6 skipped`. PASS.
  - Task 5.2 (`kind: build-check`): Observed `EXIT=0` from `... -t "cross-team send via to_agent_name"` → `Tests  1 passed | 7 skipped`. PASS.
  - Task 5.3 (`kind: build-check`): Observed `EXIT=0` from `... -t "recipients always holds"` → `Tests  1 passed | 7 skipped`. PASS.
  - No `manual-verify`, no `integration-test`, no `skip-*` tasks.
- **Sub-check B (project viability_commands)**: `openspec/project.md` does not exist; no `viability_commands` declared. Implicit build-check exercised via Task 4.1's `pnpm build` (re-run this iteration: EXIT 0). See SUGGESTION-1.
- **Sub-check C (Runtime Assumptions)**: `design.md §Runtime Assumptions` explicitly records "No triggers found during the Runtime Assumption Audit" with three justifications. Scan for audit-trigger patterns returns no unaccounted matches. PASS.

### WARN (non-blocking, inherited)

**WARN-1 — Scenario Coverage Matrix uses `:TBD` line placeholders.**
All 13 rows list test files correctly but show `<file>:TBD` instead of real line numbers. Doc-polish issue, not a coverage gap.

**WARN-2 — Auto-poke branch for same-team name-path not directly asserted.**
Spec scenario `Same-team send via to_agent_name persists and auto-pokes` specifies `poked: true`, but every test in `send-message-by-name.test.ts` uses `auto_poke: false`. Coverage is transitive via the UUID-path auto-poke tests.

**WARN-3 — Archive-ordering precondition.**
`refactor-mailbox-routing` must archive before this change. Enforced at archive time by the driver, not an apply-phase defect.

### SUGGESTION

**SUGGESTION-1 — No `openspec/project.md` viability_commands section.**
Consider adding a `viability_commands:` declaration for future changes.

### Final Assessment

0 critical issue(s) found. Prior iteration's 3 CRITICALs of type `tdd-discipline-red-shows-pass-fraud` have been resolved via Section 5 fix-tasks (kind reclassification per ts-apply §13b Option A). 3 warnings (inherited) and 1 suggestion are non-blocking. Ready for archive pending `refactor-mailbox-routing` archival ordering (WARN-3).

STATUS: pass
