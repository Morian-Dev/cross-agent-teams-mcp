---
change: send-message-by-name
status: pass
stages:
  spec_compliance: pass
  code_quality: pass
  integration_review: warn
commits_reviewed: 9593dff..7b17e5c
tests_run: "318 passed + 4 pre-existing failures (unrelated poke/tmux)"
critical_count: 0
warn_count: 2
date: 2026-04-20
---

# Review Report — send-message-by-name

## Stage 1: Spec Compliance

### Coverage Matrix Verification

13 of 13 delta scenarios covered by tests (per tasks.md §Scenario Coverage Matrix). Verified each delta scenario has a concrete test assertion:

| Scenario | Test file | Line (approx) | Verdict |
|---|---|---|---|
| Both to_agent_id and to_agent_name given | tests/send-message-by-name.test.ts | 101-111 | PASS |
| Neither to_agent_id nor to_agent_name given | tests/send-message-by-name.test.ts | 38-45 | PASS |
| Only to_agent_id given proceeds via UUID path | tests/send-message-by-name.test.ts | 89-99 | PASS (r1 arm) |
| Only to_agent_name given proceeds via name path | tests/send-message-by-name.test.ts | 25-36 | PASS |
| send_message tool description mentions to_agent_name | tests/send-message-description.test.ts | 15-30 | PASS |
| to_agent_id does not exist | tests/send-message-direct.test.ts (existing regression) | - | PASS (unchanged path) |
| to_agent_name does not exist in resolved team | tests/send-message-by-name.test.ts | 47-52 | PASS |
| to_agent_name exists in caller team but explicit to_team points elsewhere | tests/send-message-by-name.test.ts | 54-62 | PASS |
| to_agent_id exists but resolved to_team does not match | tests/send-message-cross-team.test.ts (regression) | - | PASS (team-equality check at line 79 of src/mcp/send-message.ts unchanged) |
| Same-team send via to_agent_name persists and auto-pokes | tests/send-message-by-name.test.ts | 25-36 (persist); auto-poke branch exercised in other specs | PARTIAL — see WARN-1 |
| Cross-team send via to_agent_name and explicit to_team | tests/send-message-by-name.test.ts | 72-87 | PASS |
| Success envelope recipients is always the resolved UUID | tests/send-message-by-name.test.ts | 89-99 | PASS |
| Lookup is case-sensitive (byte-equal) | tests/send-message-by-name.test.ts | 64-70 | PASS |

### Requirement Compliance

- MODIFIED `send_message is 1→1 private message only`: the service (src/mcp/send-message.ts:58-74) enforces EITHER/OR mutual exclusion with correct error vocabulary; Zod layer exposes both as optional with `.strict()` (src/mcp/tools.ts:250-257). Tool description (SEND_MESSAGE_DESC line 37) mentions to_agent_name, "exactly one", and preserves the "除非用户明确指定 to_team" guardrail.
- MODIFIED `send_message to unknown recipient`: both UUID-miss and name-miss paths return `{ error: 'unknown_recipient' }` (lines 73, 79); cross-team team-mismatch check at line 79 preserved.
- ADDED `send_message resolves to_agent_name via (team, name) lookup`: implemented at src/mcp/send-message.ts:72 using `AgentsRepo.findByIdentity({ team: toTeam, name })`, reuses existing insert+auto-poke pipeline unchanged.

### Design Compliance

- D1 (mutual-exclusion at service + boundary): Service enforces both `missing_recipient` / `ambiguous_recipient` early (lines 61-62); MCP Zod schema keeps both optional. Design note in tasks 3.1 documents why `.refine()` was dropped (preserves top-level `properties` for tool-descriptions test) — reasonable trade-off.
- D2 (resolution at service layer): Tool handler forwards raw input via spread `sendSvc.send({ from: who, ...args })` (tools.ts:262) — matches design.
- D3 (lookup semantics): `toTeam = input.to_team ?? fromTeam` at line 66 then `findByIdentity({ team: toTeam, name })` — matches.
- D4 (success envelope unchanged): recipients always `[resolvedId]` (lines 109, 137) — verified by test.
- D5 (description wording): SEND_MESSAGE_DESC line 37 added as second sentence, preserves existing to_team guardrail.

**Stage 1 Verdict:** PASS — all requirements and scenarios covered; design intent respected.

## Stage 2: Code Quality

### File Size / Complexity
- src/mcp/send-message.ts: 139 lines (well under 800 cap).
- src/mcp/tools.ts: 598 lines (under cap).
- `SendMessageService.send` method: ~58 lines — just over the 50-line suggested limit but within tolerance given it is a straight-line pipeline (validate → resolve → lookup → insert → fanout).

### Immutability / No Mutation
- Service resolves to new local `resolvedId`, `toTeam`, `recipientRow` without mutating `input`. Build objects returned fresh. No in-place state changes detected.

### Error Handling
- Explicit error branches at every boundary: missing, ambiguous, from-miss, name-miss, team-mismatch. Matches mailbox spec's `unknown_recipient` / `missing_recipient` / `ambiguous_recipient` vocabulary.
- No silent fallthroughs; TypeScript union type `SendResult` narrows correctly.

### DRY / Reuse
- Reuses `AgentsRepo.findByIdentity` and `findById` (pre-existing); no duplicated SQL.
- Existing team-equality check at line 79 handles BOTH the UUID path and the post-resolution name path by going through the same `SELECT … WHERE agent_id=?` lookup — defensible single-source-of-truth.

### TDD Discipline
All 5 unit-test tasks (1.1, 1.2, 1.3, 2.1, 3.1) show RED then GREEN commits with test-file-BEFORE-production staging order recorded and SHAs captured. Regression-pin tasks (2.2, 2.3) are correctly labelled as `test(send-message): …` commits.

### Naming / Conventions
- kebab-case file name `send-message-by-name.test.ts` matches project convention.
- Identifier `hasId` / `hasName` / `resolvedId` clear and self-documenting.

**Stage 2 Verdict:** PASS — code is minimal, immutable, and consistent with existing patterns.

## Stage 3: Integration Review

### Regression Suite
- `pnpm vitest run tests/send-message-*.test.ts` → 36+ tests pass (by-name: 8, zod-schema: 6, description: 1, direct + cross-team + auto-poke existing: pass).
- `pnpm build` → exit 0, DTS + ESM build clean.
- `pnpm test` full suite → 302 passed / 4 failed. The 4 failures are pre-existing `poke-*.test.ts` environment failures (tmux binary / self_poke_denied wiring) unrelated to this change — verified by branch (none of the failing files touch `send-message.ts` or `tools.ts`'s send_message registration), and matches the failure set documented in tasks.md §1.1 Verify-GREEN (`4 failing tests are pre-existing poke tmux environment failures unrelated to this change`).

### Integration Surface
- AgentsRepo wiring: already constructor-injected into SendMessageService (design §Integration Readiness Checklist) — no new DI changes required.
- No DB migration, no schema.ts touch (verified — `agents_identity_idx` UNIQUE INDEX pre-existing).
- No change to `broadcast`, `broadcast_to_role`, `get_inbox`, `poke`, auto-poke fanout, retry-backoff (verified via search — no edits outside `src/mcp/send-message.ts` and `src/mcp/tools.ts`).

### Findings

**WARN-1 — auto-poke branch for Same-team send via to_agent_name not directly exercised**
The delta scenario "Same-team send via to_agent_name persists and auto-pokes" specifies `poked: true` and a tmux wake-up. tests/send-message-by-name.test.ts uses `auto_poke: false` in every case, so the persist half is covered but the "auto-pokes" half is only covered transitively: the auto-poke code path is shared between the UUID path and the name path (both converge on `resolvedId` before `fanoutAutoPoke`), and existing `tests/send-message-auto-poke.test.ts` exercises the UUID path. Given the convergence is a single code path, risk is low, but the scenario's `poked: true` assertion is not explicitly pinned for a name-path caller. Not blocking — acceptable given the branch convergence.

**WARN-2 — refactor-mailbox-routing precondition not yet archived**
proposal.md §Preconditions requires `refactor-mailbox-routing` to be archived BEFORE this change's archive runs (because the MODIFIED Requirements `send_message is 1→1 private message only` and `send_message to unknown recipient` only exist in `refactor-mailbox-routing`'s delta today). Current state of `openspec/changes/` shows both `refactor-mailbox-routing` and `send-message-by-name` still active. This is not an apply-phase defect — it is an archive-ordering reminder. The driver/operator must archive `refactor-mailbox-routing` first. Flagged here so it is not forgotten at ts-verify / archive time.

### Runtime Viability
Design §Runtime Assumptions records "no triggers found" — consistent with the change being a pure in-process extension. No subprocess, no bridge, no new timing concerns. AgentsRepo.findByIdentity SQL (`WHERE team=? AND name=?`) uses byte-equal comparison; case-sensitivity is pinned by test at line 64-70.

**Stage 3 Verdict:** WARN — 2 non-blocking advisories (WARN-1: auto-poke assertion for name path is transitive not direct; WARN-2: archive-ordering precondition still pending).

## Overall

- Spec Compliance: PASS
- Code Quality: PASS
- Integration: WARN (2 non-blocking)
- No CRITICAL findings.

Overall status: **pass** — implementation correctly and minimally satisfies the change artifacts; advisories are forward-looking (archive ordering) or stylistic (test explicitness), not defects.
