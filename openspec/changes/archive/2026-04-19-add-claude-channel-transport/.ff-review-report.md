---
status: warn
change: add-claude-channel-transport
generated_at: 2026-04-19T00:00:00Z
stages:
  spec_compliance: { status: pass, issue_count: 0 }
  code_quality:    { status: warn, issue_count: 2 }
  integration:     { status: warn, issue_count: 3 }
issue_count: 5
warn_issues:
  - stage: code_quality
    id: CQ-01
    tag: legacy-branch-dead-code
    file: src/mcp/poke.ts
    line: 136
    summary: "Post-pivot the daemon always constructs a ChannelWakeFanout in server.ts:36, so the legacy `if (!fanout)` branch in poke.ts (lines 136-151) is dead code; only the test callers that omit fanout exercise it. Consider removing or document as test-only."
  - stage: code_quality
    id: CQ-02
    tag: function-length
    file: plugins/ts-agent-teams-channel/src/daemon-client.ts
    line: 39
    summary: "runRegistrationSequence is 56 lines (39-94), slightly over the <50 line guideline; could split into connect + two tool-call helpers. Not blocking."
  - stage: integration
    id: INT-01
    tag: dev-db-cruft
    file: src/mcp/poke.ts
    line: 111
    summary: "Stale `channel-proxy-<pid>-<rand>` rows from the pre-1969c34 suffix bug persist in dev DB (~100 rows). Not a correctness risk (they're offline and keyed by unique (team,name)); fresh-boot will clear. Worth a one-line cleanup in a future chore; not blocking."
  - stage: integration
    id: INT-02
    tag: scenario-missing-test
    file: openspec/changes/add-claude-channel-transport/specs/claude-channel-transport/spec.md
    line: 164
    summary: "Scenario `proxy drops relay without crashing when host stdio is closed` is covered by plugins/ts-agent-teams-channel/tests/proxy-relay.test.ts:33 but the proxy-reconnect scenario in the spec (lines 170-179) claims 2s SLA and only tests/proxy-reconnect.test.ts verifies re-execution of register+subscribe; no explicit 2s deadline assertion. Adequate for behavior, looser on timing."
  - stage: integration
    id: INT-03
    tag: proxy-identity-key-risk
    file: plugins/ts-agent-teams-channel/src/daemon-client.ts
    line: 63
    summary: "Proxy registers as (team='default', name='channel-proxy-<pid>'). Because tighten-agent-identity made (team,name) the UNIQUE key and role is informational, a non-proxy agent accidentally named 'channel-proxy-<pid>' under 'default' team would collide and be coerced to role '__channel_proxy__' on re-register. Extremely unlikely in practice, but worth a single defensive note in the identity design."
---

## Code Review: add-claude-channel-transport

### Summary
| Stage              | Status             |
|-------------------|--------------------|
| Spec Compliance   | PASS (0 findings)  |
| Code Quality      | WARN (2 findings)  |
| Integration Review| WARN (3 findings)  |

### Stage 1: Spec Compliance

Requirements: 11/11 mapped to implementation.
Scenarios: all covered in tests or manual-verify evidence.

- ADDED `channel_session_id` column → `src/storage/schema.ts:25` + tests/agents-channel-session-id-column.test.ts.
- `list_agents` returns csid → `src/storage/agents-repo.ts:53-62` + tests/agents-repo-list-channel-session-id.test.ts.
- `ChannelWakeFanout` (attach / send / detach / detachBySession / re-sub) → `src/daemon/channel-wake-fanout.ts` + tests/channel-wake-fanout.test.ts.
- `subscribe_channel_wake` role gating + session-close cleanup → `src/mcp/subscribe-channel-wake.ts` + tests/subscribe-channel-wake.test.ts.
- `bind_channel` self-binding (after pivot) → `src/mcp/bind-channel.ts` + tests/bind-channel.test.ts. Accepts only `{channel_session_id}`, resolves caller from MCP session, rejects proxy role, validates csid against live fanout.
- `sendChannelWake` + meta key filter → `src/daemon/channel-wake-send.ts` + tests/channel-wake-send.test.ts.
- Proxy startup sequence (register → subscribe → emit startup `<channel>`) → `plugins/ts-agent-teams-channel/src/cli.ts`, `daemon-client.ts` + proxy-cli.test.ts + proxy-startup-notification.test.ts.
- Proxy reconnect → `daemon-client.ts:96` + tests/proxy-reconnect.test.ts (validates re-execution; 2s SLA not explicitly asserted — see INT-02).
- E2E poke via channel transport → `src/mcp/transport-dispatch.ts` + tests/e2e-channel-poke.test.ts + manual-verify evidence runtime-idle-pane.txt / runtime-midturn-pane.txt.
- MODIFIED `poke` dispatch + `transport_used` → `src/mcp/poke.ts:111` + tests/poke-channel-transport.test.ts.
- Delta taxonomy ADDED/MODIFIED is correct. Nothing REMOVED.

### Stage 2: Code Quality

Production files reviewed: 11 (3 new + 8 modified in src, 3 in plugin).
Test files reviewed: 11.

- **CQ-01** (WARN): `src/mcp/poke.ts:136-151` legacy `if (!fanout)` branch is effectively dead in production (server.ts:36 always supplies a fanout). Only test fixtures that omit it hit this path. Clean-up candidate but preserves backwards compatibility for any external library caller.
- **CQ-02** (WARN): `runRegistrationSequence` is ~56 lines. Minor.
- All new production files are ≤ 160 LOC; tools.ts grew to 596 (well under 800). Kebab-case filenames throughout. No hardcoded secrets, no mutation in the new code. Error handling is defensive at boundaries (proxy host-close swallows; daemon sink send swallows per spec).
- Tests assert real behavior (sink payload equality, DB row state, tool-result shape), not forwarding. No forwarding-only tests detected.
- Manual-verify kind selection appropriate for 10.1 (Claude Code channel acceptance is genuinely non-unit-testable). Pure logic parts already have unit tests.

### Stage 3: Integration Review

Bridge types detected: External subprocess (channel proxy spawned by Claude Code via .mcp.json), RPC/IPC (HTTP MCP daemon ↔ stdio MCP proxy), Delegate/observer (ChannelWakeFanout sinks), session lifecycle (transport.onclose → detachBySession).
Forwarding-only tests: 0 found.
Observed output blocks reviewed: 30+ tasks, all contain plausible RED failure output and GREEN success output; manual-verify evidence files non-empty and signed with PASS verdict.
Runtime Assumptions cross-check: design.md has no named `Runtime Assumptions` section; the "Risks & Trade-offs" table covers equivalent material. Acceptable given this change explicitly runtime-verified (task 10.1) the one mid-turn assumption.

- **INT-01** (WARN): stale `channel-proxy-<pid>-<rand>` rows from pre-1969c34 fix sit in dev DB. Not a correctness issue going forward — the fix is committed, UNIQUE(team,name) index prevents future proliferation, offline filter hides them from relevant paths. Could add a cleanup SQL or migration note.
- **INT-02** (WARN): `proxy-reconnect.test.ts` verifies sequence re-execution within 4s deadline, but spec asserts "within 2 seconds". Test would pass with slower scheduling too — consider tightening.
- **INT-03** (WARN): Identity collision risk with `channel-proxy-<pid>` name in team=`default`. Given tighten-agent-identity narrowed identity to (team,name), a user registering a name matching this pattern would collide. Improbable, worth a defensive comment in `daemon-client.ts:63` or namespacing the proxy name.
- Poke flow correctness: self-poke check at `src/mcp/poke.ts:124` and cross-team check at `:130` correctly run BEFORE transport dispatch at `:153`. `allowCrossTeam` honored for internal auto-poke path unchanged.
- Role gating symmetry: `subscribe_channel_wake` requires `role=__channel_proxy__`; `bind_channel` rejects that same role. Consistent — the two tools are mutually exclusive by caller identity, matching design D7.
- `hasUsableTransportId` was rolled back to `hasUsableTmuxPaneId` per section 12 pivot. `register_agent` no longer accepts `channel_session_id` (zod schema at tools.ts:184-190 confirmed). Hint text at tools.ts:218 accurately directs channel-plugin users to bind_channel, not register_agent.
- Transport-dispatch module correctly short-circuits on successful channel send (transport-dispatch.ts:51-54) — no double poke.
- `channelWakeFanout.detachBySession` wired at `src/mcp/transport.ts:83-85` on `transport.onclose`. Correct cleanup path.

### Findings by Severity

**CRITICAL** (must fix): none.

**WARNING** (should fix):
- CQ-01: Consider pruning legacy no-fanout branch in `src/mcp/poke.ts:136-151` or gating behind a test-only export.
- CQ-02: `runRegistrationSequence` slightly over 50-line guideline — split into connect + register-agent + subscribe helpers if refactoring.
- INT-01: Cleanup note/migration for pre-fix stale `channel-proxy-*-<rand>` rows (cosmetic; fresh-boot clears).
- INT-02: Tighten proxy-reconnect test assertion to the 2-second SLA stated in spec.
- INT-03: Add defensive note or namespacing to proxy `name` to guard against future (team,name) collision.

**SUGGESTION** (consider): none additional.

### Verdict

No critical issues. 5 warnings to address (2 code-quality, 3 integration); none block proceeding. Pre-existing 4 poke-baseline failures noted in task 11.1 / 12.8 confirmed to be baseline, not introduced by this change. Ready for `/ts-verify` when ready.

STATUS: warn
