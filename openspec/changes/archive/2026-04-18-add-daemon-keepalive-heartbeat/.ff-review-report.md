---
status: pass
change: add-daemon-keepalive-heartbeat
generated_at: 2026-04-18T16:07:00Z
stages:
  spec_compliance: { status: pass, issue_count: 0 }
  code_quality:    { status: pass, issue_count: 0 }
  integration:     { status: pass, issue_count: 0 }
issue_count: 0
critical_issues: []
warn_issues: []
---

## Code Review: add-daemon-keepalive-heartbeat

### Summary
| Stage              | Status  |
|--------------------|---------|
| Spec Compliance    | PASS    |
| Code Quality       | PASS    |
| Integration Review | PASS    |

### Stage 1: Spec Compliance

Requirements: 2/2 mapped to implementation
Scenarios: 8/8 covered in code, 8/8 verified in tests (plus manual-verify for docs)

Requirement mapping:

- **HTTP keep-alive timeout default and env override** → `src/daemon/server.ts:12-22` (`DEFAULT_KEEP_ALIVE_TIMEOUT_MS`, `parsePositiveInt`, Fastify option, `headersTimeout = keepAliveTimeout + 1000`).
- **SSE heartbeat ticker on attached sinks** → `src/daemon/sse-fanout.ts:11-91` (interval resolution, attach/detach lifecycle, start/stopHeartbeat, unref) + `src/mcp/transport.ts:42-48` (`sendHeartbeat()` sink method emits `notifications/heartbeat`) + `src/daemon/server.ts:39-43` (`onClose` → `fanout.stopAll()` clears timer).

Scenario coverage:

| Scenario | Test |
|---|---|
| Default keep-alive timeout when no env var | `tests/daemon-keepalive-timeout.test.ts:18-25` |
| Env override applies at boot | `tests/daemon-keepalive-timeout.test.ts:27-34` |
| Invalid env value falls back to default | `tests/daemon-keepalive-timeout.test.ts:36-42` |
| First attach starts the ticker | `tests/sse-fanout-heartbeat.test.ts:8-18` (implicit via tick count > 0) + `tests/mcp-heartbeat-e2e.test.ts` |
| Last detach stops the ticker | `tests/sse-fanout-heartbeat.test.ts:20-30` |
| Heartbeat delivered at configured interval | `tests/sse-fanout-heartbeat.test.ts:8-18` |
| MCP client receives notifications/heartbeat | `tests/mcp-heartbeat-e2e.test.ts:27-46` |
| Heartbeat does not interfere with contract_event delivery | `tests/sse-fanout-coexistence.test.ts:44-95` |

No findings.

### Stage 2: Code Quality

Production files reviewed: 3 (`src/daemon/server.ts`, `src/daemon/sse-fanout.ts`, `src/mcp/transport.ts`).
Test files reviewed: 5 (3 new + 1 modified coexistence/fanout + 1 fixup).

- Naming: `DEFAULT_KEEP_ALIVE_TIMEOUT_MS`, `DEFAULT_HEARTBEAT_INTERVAL_MS`, `parsePositiveInt`, `resolveHeartbeatIntervalMs`, `startHeartbeat` / `stopHeartbeat` — all behavior-descriptive and consistent with existing project style.
- Single responsibility: `parsePositiveInt` is a pure parser; `resolveHeartbeatIntervalMs` wraps constructor-arg vs env precedence cleanly; `startHeartbeat` / `stopHeartbeat` are a minimal symmetric pair.
- Error handling: `sendHeartbeat` wraps `transport.send(...).catch(() => {})` per spec's "best-effort" requirement; `setInterval` callback catches per-sink errors so one broken sink doesn't kill the tick.
- No duplication; no YAGNI (every knob is required by a scenario); no dead code.
- Test quality: unit tests in `tests/sse-fanout-heartbeat.test.ts` use `vi.useFakeTimers()` with explicit spy assertions on `sendHeartbeat` call count (not a forwarding-only shape — they pin the number of ticks). E2E tests (`tests/mcp-heartbeat-e2e.test.ts`, `tests/sse-fanout-coexistence.test.ts`) assert on notifications actually received by the real SDK client over HTTP, which is the canonical integration surface.
- Verification kind sanity: task 3.1 is `manual-verify` for a docs-only change — correct choice, nothing to extract for unit testing. Task 1.1 is `integration-test` (must boot Fastify to read `app.server.keepAliveTimeout`) — correct choice. Task 2.1 is `unit-test` with fake timers — correct.

No findings.

### Stage 3: Integration Review

Bridge types detected:

- **External subprocess / RPC boundary** — partial match: we're not spawning a subprocess, but we ARE configuring the Node HTTP server connection pool (socket keep-alive) that talks to remote MCP clients over TCP. Applied the relevant subset of the RPC / IPC checklist.
- **Delegate / observer registration** — `SseFanout.attach()` registers sinks; `transport.onclose` → `fanout.detach()` + `sessions.delete()`.
- **Threading boundary** — heartbeat `setInterval` ticker interleaves with handler-invoked `emitContractEvent` on the main event loop. `.unref()` applied to avoid holding process open.

Bridge audit findings:

- External/RPC — Resource cleanup: `onClose` hook calls `fanout.stopAll()` (clears heartbeat timer and closes sinks), `clearInterval(interval)` for cleanup loop, then `db.close()`. Full cleanup chain present (`src/daemon/server.ts:39-43`).
- External/RPC — Encoding at boundary: heartbeat payload is JSON-RPC via SDK `transport.send()` — no raw socket writing, encoding handled by SDK.
- External/RPC — Timeout: `keepAliveTimeout` explicit, `headersTimeout = keepAliveTimeout + 1000` satisfies Node's invariant (documented in design.md risk table).
- Delegate — Weak-ref direction: not applicable (JS has no weak references in this path; the sink closes itself via `transport.onclose`).
- Delegate — Lifecycle cleanup: `detach` on `transport.onclose` verified in `src/mcp/transport.ts:54-60`; `stopAll()` on daemon shutdown; `stopHeartbeat` when last sink detaches (prevents idle-daemon timer waste per design.md Key Decision 4).
- Threading — Ordering: heartbeat and contract_event both use `setInterval` / synchronous `emitContractEvent` callbacks on the main event loop with serial `transport.send()` calls. `tests/sse-fanout-coexistence.test.ts:44-95` proves no interference by asserting both notification streams arrive independently. This is the canonical sanity check for this kind of shared-transport interleave.

Forwarding-only test audit: 0 forwarding-only tests detected. `tests/sse-fanout-heartbeat.test.ts` uses spies but asserts on call counts (the actual behavior — "timer fires N times in 250ms"), not on "the argument I passed came back". E2E tests assert on real client-received notifications.

Observed output block review: 5/5 tasks with observed-output blocks contain plausible vitest output. Task 2.2 RED was specifically verified by reverting sse-fanout.ts to pre-2.1 state (documented in tasks.md:337-349), which produced the expected failure mode and confirms the e2e test is not a tautology. Task 2.3 RED was correctly noted as "regression-guard — passes immediately after 2.1" which is honest and consistent with the scenario's non-interference framing.

Runtime Assumption cross-check: design.md has no dedicated `## Runtime Assumptions` section, but three assumption-style statements appear in the Key Decisions:

1. "MCP spec: clients MUST tolerate unknown notification methods by dropping silently" — relied on by `notifications/heartbeat`. Verified empirically by the e2e test (which registers an explicit handler, but the handler only increments a counter — unknown-notification silent-drop semantics are further exercised by the coexistence test where client B connects and registers no heartbeat handler, implicitly proving silent-drop is tolerable).
2. "SDK's `transport.send()` already catches 'no active GET stream yet' errors" — relied on by `sendHeartbeat()` via `.catch(() => {})`. The explicit `.catch` in `src/mcp/transport.ts:46` hedges even further; defensive and correct.
3. "Node requirement: headersTimeout > keepAliveTimeout" — reflected in `src/daemon/server.ts:22`.

None of these is a blocking gap, but they would benefit from a formal `Runtime Assumptions` section in design.md. Treating as SUGGESTION-class observation only (not a finding worth blocking on — the assumptions are empirically verified by tests).

Missing Runtime Assumptions scan: design.md text contains "rely on" / "default" phrasings but each is accompanied by a verification path (either a test or an explicit defensive code guard). No unaccounted-for external default reliance detected.

### Findings by Severity

**CRITICAL**: none.

**WARNING**: none.

**SUGGESTION** (non-blocking observation):
- design.md does not have a dedicated `## Runtime Assumptions` section. The three assumptions (MCP unknown-notification silent-drop, SDK transport.send swallow semantics, Node headersTimeout invariant) are each backed by tests or defensive code, so this is not a review blocker — but a future `## Runtime Assumptions` block would make the contract with external libraries (`@modelcontextprotocol/sdk`, Node HTTP) explicit for long-term maintenance.

### Verdict

Review passed. No critical issues, no warnings. Ready for `/ts-verify` and archive.

STATUS: pass
