---
change: fix-fanout-skip-offline
status: pass
date: 2026-04-19
dimensions:
  completeness: pass
  correctness: pass
  coherence: pass
  test_adequacy: pass
  tdd_discipline: pass
  runtime_viability: pass
critical: 0
warn: 0
---

# Verify Report — fix-fanout-skip-offline

## 1. Completeness: PASS

All 6 tasks (1.1, 2.1, 3.1, 4.1, 4.2, 4.3) in `tasks.md` marked `[x]`. Every RED/GREEN/REFACTOR block has concrete observed output (test counts, exit codes, grep counts). No placeholders or TODO markers left unresolved. Task 1.1 explicitly documented as refactor-export with a trivial assertion test; all other unit-test tasks provide RED failure diff.

## 2. Correctness: PASS

- `src/mcp/broadcast.ts:46-48` — adds `AND last_seen_at > ?` with `cutoffIso = new Date(Date.now() - ONLINE_MS).toISOString()`. Matches spec delta exactly.
- `src/mcp/send-message.ts:65-67` — to_role branch adds `AND last_seen_at > ?` with the same cutoff derivation.
- `src/mcp/send-message.ts:58-63` — to_agent_id branch preserved byte-identical (`SELECT agent_id, tmux_pane_id FROM agents WHERE agent_id=? AND team=?`, no `last_seen_at` predicate, no `cutoffIso` binding). Verified unchanged.
- `src/storage/agents-repo.ts:22` — `ONLINE_MS` is module-level `export const`, value `5 * 60 * 1000`.

## 3. Coherence: PASS

Proposal, design, spec delta, tasks, and code all consistently state:
- 5-min threshold via exported `ONLINE_MS` constant.
- ISO cutoff computed in JS (lexicographic string comparison safe).
- Single-recipient `to_agent_id` exempt from filter (preserves Offline delivery contract).
- `list_agents` unchanged (still returns ghosts with `online` flag).
- Empty recipient list returns `{ error: "unknown_recipient" }` (no new error code).

Tool descriptions in `src/mcp/tools.ts:206-207,241` document both the filter and the `to_agent_id` exception, matching the spec's "MCP tool descriptions" scenario.

## 4. Test Adequacy: PASS

All 7 spec scenarios covered by `tests/fanout-skip-offline.test.ts` (6 behavioral + ONLINE_MS sanity) and `tests/tool-descriptions-poke-hint.test.ts` (description-filter assertions):
- Broadcast mixed online/offline → recipients `[B,D]`, C has zero mailbox rows, event payload recipients match (uses direct `UPDATE agents SET last_seen_at=?` to simulate offline).
- Broadcast all-offline → unknown_recipient, zero new events/messages.
- list_agents ghosts still returned with `online: false`.
- send_message to_role mixed → `[F1,F3]`, F2 has zero mailbox rows.
- send_message to_role all-offline → unknown_recipient.
- send_message to_agent_id with 3-hour-old last_seen_at still delivers (single-recipient bypass explicitly exercised at line 139-160 with full event/message persistence assertions).
- Tool-description scenario covered by 2 new assertions in `tests/tool-descriptions-poke-hint.test.ts`.

## 5. TDD Discipline: PASS

`.ff-trace/apply-1.log` shows RED failure output before GREEN for every TDD task:
- Task 1.1 RED: `expected undefined to be 300000` (1 failed).
- Tasks 2.1/3.1 RED: 4 failed | 3 passed (mixed offline broadcast + to_role failures pre-fix, with to_agent_id already passing by design).
- Task 4.1 RED: `expected description to match /to_agent_id/i` (1 failed).
Each RED followed by concrete GREEN output and deliberate REFACTOR (noop with YAGNI rationale).

## 6. Runtime Viability: PASS

- `pnpm exec vitest run --reporter=verbose` → Test Files 66 passed (66), Tests 189 passed (189), 5.69s. 100% green.
- `pnpm run build` → ESM + DTS build success, dist/cli.js 63.63 KB.
- `grep -c "last_seen_at >" dist/cli.js` = 3 (≥ 2): line 450 (send-message to_role), line 539 (broadcast), line 1558 (pre-existing contract-subs SELECT). Both new filter sites are in the compiled artifact.

## Pre-existing items (not raised)

- tmux-cli/poke-guard/poke-retry tsc resolution diagnostics (vitest green).
- `sse-fanout.ts:22`, `broadcast.ts:39` unused vars.
- Editor stale TS cache on `online-threshold.test.ts`.

## Findings

No CRITICAL. No WARN. Implementation is ready for archive.
