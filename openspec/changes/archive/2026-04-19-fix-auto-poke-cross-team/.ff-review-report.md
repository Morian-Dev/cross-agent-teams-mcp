---
status: pass
change: fix-auto-poke-cross-team
generated_at: 2026-04-19T21:00:00Z
stages:
  spec_compliance: { status: pass, issue_count: 0 }
  code_quality:    { status: pass, issue_count: 0 }
  integration:     { status: pass, issue_count: 0 }
issue_count: 0
---

## Code Review: fix-auto-poke-cross-team

### Summary
| Stage              | Status              |
|-------------------|---------------------|
| Spec Compliance   | PASS / 0 findings   |
| Code Quality      | PASS / 0 findings   |
| Integration Review| PASS / 0 findings   |

### Stage 1: Spec Compliance

Requirements mapped to implementation: 2/2
- `Cross-team poke via the MCP tool is rejected` → `src/mcp/poke.ts:96` (`callerRow.team !== target.team && !deps.allowCrossTeam`) + MCP entry at `src/mcp/tools.ts:491` (does not pass the flag).
- `Internal auto-poke bypasses the cross-team check` → `src/mcp/tools.ts:77` passes `allowCrossTeam: true`; hint format preserved by `buildAutoPokeHint` (`src/mcp/tools.ts:56-65`).

Scenarios covered in tests: 3/3
- Scenario `Cross-team target via MCP tool` → verified by `tests/poke-validation.test.ts:119` and `tests/poke-cross-team-allow-flag.test.ts:43` (first assertion).
- Scenario `Cross-team send_message triggers a successful auto-poke` → verified at `createAutoPokeImpl` bridge level by `tests/auto-poke-impl-cross-team.test.ts:37` (asserts `{ ok: true }`, not `guard_failed`). Note: the end-to-end send_message integration assertion on paste-buffer/send-keys is not directly asserted by a new test in this change, but is covered transitively through the existing `tests/send-message-cross-team-auto-poke.test.ts` plus the new bridge test — acceptable per design D4.
- Scenario `Direct MCP poke call with the same cross-team pair still denied` → verified by `tests/poke-validation.test.ts:119` and `tests/poke-cross-team-allow-flag.test.ts:43`.

### Stage 2: Code Quality

Production files reviewed: 2 (`src/mcp/poke.ts`, `src/mcp/tools.ts`)
Test files reviewed: 2 (`tests/poke-cross-team-allow-flag.test.ts`, `tests/auto-poke-impl-cross-team.test.ts`)

- `PokeDeps` addition is minimal, non-leaky: `allowCrossTeam?: boolean` with a single-line JSDoc-style caveat on line 11 ("for internal auto-poke callers only; MCP tool entry MUST NOT pass it"). No YAGNI — it satisfies a concrete spec scenario.
- Condition at `src/mcp/poke.ts:96` uses short-circuit `&& !deps.allowCrossTeam` — defaults to current behavior when flag absent (matches Runtime Assumption A1).
- `createAutoPokeImpl` (`tools.ts:67-87`) is the only caller that passes `allowCrossTeam: true`, and the `hint` it passes is the constrained `buildAutoPokeHint` output — satisfies spec's "prompt MUST be the constrained hint format" constraint.
- Test quality: both new tests use real `poke()` / `createAutoPokeImpl` bindings (not mocks of the internal functions); they mock only the tmux-cli boundary. Assertions target behavior (`{ ok: true }`, `{ error: 'cross_team_denied' }`) rather than implementation details. Each test creates an isolated tmpdir DB — no shared state.
- Naming follows project conventions (kebab-case files, descriptive `describe` / `it` labels).
- No dead code, no duplicated logic introduced.

### Stage 3: Integration Review

Bridge types detected: none of the catalogued types (no SwiftUI/UIKit, no FFI, no new subprocess, no new observer registration). The change is a pure TS flag-plumbing fix inside a single process.

Forwarding-only tests: 0. The new auto-poke bridge test asserts the real `createAutoPokeImpl`'s return value through the real `poke()` (only tmux-cli is mocked at the subprocess boundary), directly covering the D4 "previously only mock at fanout layer" gap called out in design.

Observed output blocks:
- Task 1.1 RED block shows a genuine failure ("expected false to be true") at `tests/poke-cross-team-allow-flag.test.ts:59` — plausible.
- Task 1.1 GREEN shows targeted pass + full suite delta (+1 test vs baseline) — plausible.
- Task 1.2 RED shows `{ ok: false, reason: 'guard_failed' }` vs expected `{ ok: true }` — plausible failure.
- Task 1.2 GREEN / REFACTOR show passing output — plausible.

Runtime Assumptions cross-check (A1): `allowCrossTeam?: boolean` defaulting to undefined → falsy — verified by `src/mcp/poke.ts:96` and by the first test case in `poke-cross-team-allow-flag.test.ts` (omits the flag, expects denial). A1 holds.

Regression verification (live): `npx vitest run tests/poke-cross-team-allow-flag.test.ts tests/auto-poke-impl-cross-team.test.ts tests/poke-validation.test.ts`:
- All 3 new assertions pass.
- `tests/poke-validation.test.ts:119 cross_team_denied` still passes (MCP entry unchanged).
- One pre-existing failure (`tmux_pane_not_set`) is a baseline issue from `tighten-agent-identity` (A/B clients colliding on session id) — explicitly noted in tasks.md and unrelated to this change.

### Findings by Severity

None.

### Verdict

Review passed. Spec compliance, code quality, and integration stages are all clean. The internal-only `allowCrossTeam` flag is appropriately scoped: it is not exposed via MCP `inputSchema` (tools.ts:483-486), the only caller that sets it is `createAutoPokeImpl` (which emits only the fixed hint format), and the MCP direct-call path retains `cross_team_denied`. Ready for `/ts-verify` and archive.

STATUS: pass
