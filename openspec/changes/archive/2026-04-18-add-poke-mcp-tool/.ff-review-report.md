---
status: pass
change: add-poke-mcp-tool
phase: review
iteration: 1
generated_at: 2026-04-17T00:00:00Z
stages:
  spec_compliance: { status: pass, issue_count: 0 }
  code_quality:    { status: pass, issue_count: 0 }
  integration:     { status: pass, issue_count: 0 }
issue_count: 0
---

## Code Review: add-poke-mcp-tool

### Summary
| Stage              | Status              |
|--------------------|---------------------|
| Spec Compliance    | PASS / 0 findings   |
| Code Quality       | PASS / 0 findings   |
| Integration Review | PASS / 0 findings   |

---

### Stage 1: Spec Compliance

Requirements: 11/11 mapped to implementation
Scenarios: 12/12 covered in code, 12/12 verified in tests

Scenario Coverage Matrix (verified against actual source):

| # | Spec Scenario | Impl (file:line) | Test (file:line) |
|---|---|---|---|
| 1 | poke appears in list_tools | `src/mcp/tools.ts:344-361` | `tests/poke-tool-registration.test.ts:18-40` |
| 2 | Happy path returns before/after tails | `src/mcp/poke.ts:105-117` | `tests/poke-e2e.test.ts:78-110` |
| 3 | Daemon issues tmux commands without shell | `src/daemon/tmux-cli.ts:19-47` (execFile/spawn with argv arrays; stdin for load-buffer) | `tests/tmux-cli.test.ts:16-59` (asserts argv + stdin) |
| 4 | Unregistered session rejected with unknown_agent | `src/mcp/poke.ts:76` | `tests/poke-validation.test.ts:40-52` |
| 5 | Target not in agents table | `src/mcp/poke.ts:83-86` | `tests/poke-validation.test.ts:54-67` |
| 6 | Target never registered pane id | `src/mcp/poke.ts:96` | `tests/poke-validation.test.ts:84-99` |
| 7 | Caller pokes self | `src/mcp/poke.ts:88` | `tests/poke-validation.test.ts:69-82` |
| 8 | Cross-team target | `src/mcp/poke.ts:90-94` | `tests/poke-validation.test.ts:119-134` |
| 9 | 10 KB prompt rejected before any tmux action | `src/mcp/poke.ts:78-81` (check runs before target lookup) | `tests/poke-validation.test.ts:101-117` |
| 10 | No tmux binary on PATH | `src/mcp/poke.ts:98-100` | `tests/poke-tmux-unavailable.test.ts:42-60` |
| 11 | Target pane was killed after registration | `src/mcp/poke.ts:58-64` (classifyTmuxError pane_dead tokens) | `tests/poke-e2e.test.ts:43-76` |
| 12 | load-buffer fails unexpectedly → tmux_cmd_failed w/ stage | `src/mcp/poke.ts:58-73` (classifyTmuxError + runStage wrapper) | `tests/poke-tmux-cmd-failed.test.ts:28-55` |

All 12 scenarios map one-to-one. Error precedence (unknown_agent → prompt_too_long → unknown_target → self_poke_denied → cross_team_denied → tmux_pane_not_set → tmux_unavailable → staged tmux errors) is consistent with spec intent.

No Stage 1 findings.

---

### Stage 2: Code Quality

Production files reviewed: 3 (`src/daemon/tmux-cli.ts` new, `src/mcp/poke.ts` new, `src/mcp/tools.ts` edit)
Test files reviewed: 5 (`tests/tmux-cli.test.ts`, `tests/poke-validation.test.ts`, `tests/poke-tool-registration.test.ts`, `tests/poke-e2e.test.ts`, `tests/poke-tmux-unavailable.test.ts`, `tests/poke-tmux-cmd-failed.test.ts`)

Checks that passed:
- Named constants used for magic numbers: `PROMPT_MAX_BYTES = 8192`, `PASTE_SETTLE_MS = 400`, `TAIL_LINES = 8` (poke.ts:31-33). No hard-coded magic in the hot path.
- No shell string concatenation anywhere; `tmux-cli.ts` exclusively uses `execFile`/`spawn` with argv arrays. Prompt bytes reach tmux via spawn-stdin, not argv — no command injection surface.
- Stage labels (`capture_before | load_buffer | paste_buffer | send_keys | capture_after`) are a closed union; `runStage` preserves stage context across the throw boundary; `classifyTmuxError` consumes it deterministically.
- Error envelope shape `{ error, detail? }` matches spec exactly for all 9 error codes.
- File sizes within CLAUDE.md limits (tmux-cli.ts 56 lines, poke.ts 119 lines, tools.ts 381 lines).
- No explanatory prose comments; comments present are intent/contract-level English (`// test helper only`, `// poke` section divider in tools.ts) — conforms to CLAUDE.md.
- Test isolation: each test uses `mkdtempSync` + `afterEach` rmSync cleanup, fresh daemon per test. `poke-e2e.test.ts` wraps tmux `new-session` / `kill-session` in try/finally or early-kill-before-assert, so sessions never leak even on test failure.
- Skip discipline: e2e tests call `isTmuxAvailable()` + `_resetTmuxAvailableCache()` in afterEach so cache state never crosses tests; test skips with console.warn when tmux absent (non-fatal).
- `poke-tmux-cmd-failed.test.ts` uses vi.mock at module level with `importOriginal` spread — proper mock hygiene; does not leak across files.

Minor observations (not findings, below suggestion threshold):
- `poke.ts:90-93` issues a second DB query for caller's team even though tools.ts already verified registration. Acceptable defensive coding at the unit-level boundary since `poke()` is also called directly from tests and is part of the module's public contract.
- `_resetTmuxAvailableCache` / `_setTmuxAvailableForTest` are exported for test use only and prefix-named `_` per common convention. Fine.

No Stage 2 findings.

---

### Stage 3: Integration Review

Bridge types detected:
- **External subprocess** (`execFile`/`spawn` calls to `tmux` CLI from `src/daemon/tmux-cli.ts`)

External-subprocess checklist:

| Item | Verdict | Evidence |
|---|---|---|
| Resource cleanup on disconnect/cancel/error | PASS | `paste-buffer -d` deletes the scoped buffer on success; on any tmux error the failed stage throws and the caller propagates `{error, detail}`, no orphan buffer state across daemon lifetime because buffer names are scoped `poke-<random-6>` (poke.ts:103). |
| Timeout handling | PASS (acceptable for MVP) | No explicit timeout on tmux commands, but two `setTimeout(400ms)` are named via `PASTE_SETTLE_MS` and all child processes exit via tmux's own command completion. Design explicitly deferred retry/timeout loops. |
| Error propagation | PASS | `runStage` wraps every tmux cli call, tags with stage label, rethrows as `StageError`; top-level catch translates via `classifyTmuxError`. |
| Encoding at boundary | PASS | `Buffer.from(prompt, 'utf8')` on the stdin write path (tmux-cli.ts:36); capture outputs returned as strings. |

Forwarding-only test audit: N/A. Tests use real assertions — argv arrays, envelope shape, DB state, real tmux pane before/after tails.

Observed-output block audit: Every task in tasks.md shows RED blocks with failure indicators (AssertionError, Failed to load url, SyntaxError) and matching GREEN blocks with pass counts. No evidence fraud.

Integration with existing capabilities:
- **agent-registry feed-in**: `tmux_pane_id` column read via `SELECT ... FROM agents` — matches schema defined in `openspec/specs/agent-registry/spec.md:11`. NULL→empty-string both correctly trigger `tmux_pane_not_set`.
- **mcp-transport**: Tool registered via `server.registerTool('poke', ...)` at `src/mcp/tools.ts:344`, alongside the existing 14 tools. Uses the same `getCallerAgentId` closure pattern — no deviation from the transport session→agent_id binding defined in `src/mcp/transport.ts:27-31`.
- **daemon-core shutdown**: poke uses only per-request spawn/execFile; all child processes exit by tmux command completion before the tool call resolves, so there are no lingering subprocesses when `app.close()` runs (`src/daemon/shutdown.ts:6`). The `_isTmuxAvailable` cache is module-level and resets naturally on process exit.
- **events-outbox**: Untouched — `poke.ts` has zero imports from `events-outbox.ts`, matching design Non-Goal "不新增 events outbox 条目".
- **Backward compatibility**: Pure addition — no existing tool, table, or field modified.

Concurrent-daemon concern (test-suite vs this chat's live tmux):
- e2e tests create tmux sessions with names `atm-test-dead-${process.pid}` and `atm-test-happy-${process.pid}`, where `process.pid` is the vitest worker PID — orthogonal to any interactive tmux session on the host.
- Tests explicitly target those named sessions for `kill-session`; they never invoke `kill-server` or touch arbitrary panes. The production code only touches `target.tmux_pane_id` which is an agent-self-reported field, and tests always register mock pane ids pointing to their own scratch sessions.
- Conclusion: running this test suite concurrently with a live production daemon sharing the same tmux server is safe.

Runtime Assumptions cross-check: design.md has no `## Runtime Assumptions` section. Scanned for trigger patterns (`use default`, `rely on`, `默认`, `依赖`). The only hit is `默认不加锁` at design.md:118 which describes an internal implementation choice (no cross-daemon mutex for concurrent pokes), not an external framework default — below the threshold that requires a Runtime Assumption entry.

No Stage 3 findings.

---

### Findings by Severity

**CRITICAL** (must fix): none

**WARNING** (should fix): none

**SUGGESTION** (consider): none

---

### Verdict

Review passed. No findings across spec compliance, code quality, or integration review. Ready for `/ts-verify` and archive.

STATUS: pass
