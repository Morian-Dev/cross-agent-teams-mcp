---
status: warn
change: add-auto-poke-retry-backoff
generated_at: 2026-04-18T15:53:00Z
stages:
  spec_compliance: { status: pass, issue_count: 0 }
  code_quality:    { status: warn, issue_count: 2 }
  integration:     { status: pass, issue_count: 0 }
issue_count: 2
warn_issues:
  - stage: code_quality
    id: CQ-01
    tag: duplicated-constant
    file: src/mcp/send-message.ts
    line: 31
    summary: "RETRY_DELAYS_S is duplicated as a local module constant in send-message.ts and broadcast.ts when src/mcp/poke-retry.ts already exports RETRY_DELAYS_S; import from the source to avoid drift."
  - stage: code_quality
    id: CQ-02
    tag: dead-init-pattern
    file: src/mcp/poke-retry.ts
    line: 46
    summary: "scheduleRetry creates a placeholder setTimeout(()=>{},0) and immediately clearTimeout()s it before the real enqueueNext runs; this creates and discards a timer handle every call — replace with a nullable timer field or init to 0 with proper typing."
---

## Code Review: add-auto-poke-retry-backoff

### Summary
| Stage              | Status             |
|--------------------|--------------------|
| Spec Compliance    | PASS / 0 findings  |
| Code Quality       | WARN / 2 findings  |
| Integration Review | PASS / 0 findings  |

### Stage 1: Spec Compliance

Requirements: 1/1 mapped to implementation.
Scenarios: 7/7 covered in code, 7/7 verified in tests.

Requirement mapping:

- `Auto-poke retry with backoff on guard_failed` → `src/mcp/poke-retry.ts` (core retry machinery, RETRY_DELAYS_MS `[30_000, 180_000, 600_000]`); `src/mcp/auto-poke-fanout.ts:83-103` (schedules retries only for `guard_failed` with pane_id); `src/daemon/server.ts:40-45` (onClose clears retries); `src/mcp/send-message.ts:97-106` + `src/mcp/broadcast.ts:74-83` (response fields).

Scenario coverage:

- `Guard_failed recipient schedules 3 retries` → covered in `tests/fanout-retry-scheduling.test.ts:15-38` and `tests/send-message-auto-poke.test.ts:135-153`.
- `First retry tick guard passes → poke fires, remaining cancelled` → `tests/poke-retry.test.ts:45-55`.
- `Recipient activity cancels pending retries` → `tests/poke-retry.test.ts:85-105`.
- `All 3 retries guard_fail, message remains in mailbox only` → `tests/poke-retry.test.ts:74-83`.
- `no_pane recipient does NOT get retry` → `tests/fanout-retry-scheduling.test.ts:40-59` and `tests/send-message-auto-poke.test.ts:73-89`.
- `Fan-out with mixed outcomes — only guard_failed recipients get retries` → `tests/fanout-retry-scheduling.test.ts:61-89` and `tests/send-message-auto-poke.test.ts:106-133`.
- `Shutdown clears all pending retry timers` → `tests/poke-retry.test.ts:107-118` and `tests/poke-retry-shutdown.test.ts:16-38`.

Spec tie-in details checked:

- Retry delays `[30, 180, 600]` seconds match spec: asserted in `tests/send-message-auto-poke.test.ts:129,150` and `tests/broadcast-auto-poke.test.ts:118`.
- Only `guard_failed` triggers retry — confirmed in `auto-poke-fanout.ts:87`: `res.reason === 'guard_failed' && res.paneId`.
- `last_seen_at > sent_at` → silent stop — `poke-retry.ts:73-76`.
- Missing recipient / no pane at tick → silent stop — `poke-retry.ts:68-72`.
- Successful retry does NOT emit event/message — confirmed: `pokeFn` wrapper in `auto-poke-fanout.ts:97` discards result; no event append on retry path.
- Shutdown hook clears map — `server.ts:42` inside onClose.

### Stage 2: Code Quality

Production files reviewed: `src/mcp/poke-retry.ts`, `src/mcp/auto-poke-fanout.ts`, `src/mcp/send-message.ts`, `src/mcp/broadcast.ts`, `src/mcp/tools.ts`, `src/daemon/server.ts`.
Test files reviewed: `tests/poke-retry.test.ts`, `tests/fanout-retry-scheduling.test.ts`, `tests/poke-retry-shutdown.test.ts`, `tests/send-message-auto-poke.test.ts`, `tests/broadcast-auto-poke.test.ts`, `tests/tool-descriptions-poke-hint.test.ts`.

Findings:

- **WARNING (CQ-01)** — Duplicated `RETRY_DELAYS_S` constant. `src/mcp/send-message.ts:31` and `src/mcp/broadcast.ts:27` each define a local `const RETRY_DELAYS_S = [30, 180, 600]`, while `src/mcp/poke-retry.ts:2` already exports `RETRY_DELAYS_S`. Recommendation: import from `poke-retry.js` to eliminate drift risk (if the backoff sequence ever changes, three files must stay in sync).
- **WARNING (CQ-02)** — `scheduleRetry` at `src/mcp/poke-retry.ts:43-50` allocates a dummy `setTimeout(() => {}, 0)` just to satisfy the `RetryEntry.timer` field's non-null type, then clears it one line later. This is a harmless but confusing construct; recommend typing `timer` as `ReturnType<typeof setTimeout> | null` (or initialising the entry with `attempt: 0` and setting `timer` inside `enqueueNext`). Would also remove the need for the `cancelRetry(key)` pre-call.

YAGNI check: no over-engineered abstractions. Backoff is hardcoded as design.md explicitly prescribed (no env-override, no exponential formula), matching the "Don't do `2ⁿ` / env knobs" Non-Goals. `__peekRetryMap` is test-only with `__` prefix — acceptable pattern used elsewhere in the repo.

Test quality: tests assert on pokeFn call count, pokeFn argument shape (paneId/body/target), and retry map presence/absence — no forwarding-only patterns. Fake-timer usage is correct (`vi.useFakeTimers()` / `advanceTimersByTimeAsync`). Integration tests use real `fanoutAutoPoke` with stubbed `capturePaneTail` via the existing `__setCapturePaneTail` test hook, exercising the real scheduling path.

Verification-kind sanity:
- Task 1.1 (unit-test), 2.1/3.1/4.1 (integration-test) all have automated assertions. Evidence blocks in tasks.md show plausible RED and GREEN output.
- Task 5.1 is manual-verify but is **documentation only** (tool description strings, docs/configs/README.md subsection). A proxy unit test (`tests/tool-descriptions-poke-hint.test.ts:44-62`) already asserts the description contains `retry|backoff`, `retry_scheduled`, `retry_delays_s` — which closes the pure-logic gap. No further extraction is feasible; manual-verify kind is appropriate.

### Stage 3: Integration Review

Bridge types detected:

- **External subprocess** (tmux CLI): `runQuietGuard` → `capturePaneTail` → `tmux capture-pane`. The retry path invokes `runQuietGuard(agent.tmux_pane_id)` (poke-retry.ts:77) inside a `try/catch` (poke-retry.ts:67,95-97) that deletes the map entry on any failure; this stops the retry chain cleanly if the tmux child errors. Encoding/timeout handling is inherited from the pre-existing `runQuietGuard`/`capturePaneTail` modules — not modified by this change.
- **Threading / timer boundary**: `setTimeout` callbacks from `enqueueNext` run on the Node event loop. The tick function is `async` and `await`s both `paneGuardFn` and `pokeFn`; `setTimeout(() => { void tick(key) }, delay)` (poke-retry.ts:60) correctly voids the promise to avoid unhandled rejection sidetracks. Shared state (`retryMap`) is only accessed from single-threaded JS — acceptable.
- **Fastify lifecycle hook**: `app.addHook('onClose', async () => { ... clearAllRetries() ... })` at `server.ts:40-45`. `clearAllRetries` is synchronous and iterates map + `clearTimeout`. Ordering with `interval`, `fanout.stopAll()`, and `db.close()` is fine; retries call back through `paneGuardFn` which could target a now-closed `db` via `lookupAgentFn`, but the timer is cleared before any outstanding tick can fire.

Bridge checklists:

- External subprocess — cleanup on error: **PASS** (try/catch deletes map entry); timeout: inherited; error propagation: retries swallow errors silently by design per spec line 60 ("silent giveup"); encoding: inherited.
- Delegate/observer — dependency injection of `paneGuardFn`/`pokeFn`/`lookupAgentFn` in `RetryContext`: strong refs by design (entry owns context for its lifetime); lifecycle cleanup on shutdown via `clearAllRetries` — **PASS**.
- Threading boundary — timer fires on event loop, single-threaded JS, no cross-actor state — **PASS**.

Forwarding-only test audit: none detected. All new tests assert on real observable state (pokeFn call count/args, retry map membership, response fields).

Observed output blocks: reviewed all 5 tasks; RED blocks show recognisable failure signatures (`AssertionError: expected undefined to be +0`, `Failed to load url`); GREEN blocks show test counts matching the number of cases. No evidence fraud.

Runtime Assumption cross-check: design.md has no `## Runtime Assumptions` section. Design text scan for trigger patterns (`default`, `rely on`, `不自定义`, etc.):

- Backoff `[30, 180, 600]` is explicitly called out as hardcoded with rationale; design rejects env override. This is an internal constant, not a reliance on an external framework default — no Runtime Assumptions entry required.
- `agents_repo.touch(agentId)` side-effect is "relied on" per design §3 but is internal project code, verified by `touchIfRegistered` in `tools.ts:76-82`. No external default involved.
- No missing Runtime Assumptions detected.

### Findings by Severity

**CRITICAL** (must fix): none.

**WARNING** (should fix):
- CQ-01 `src/mcp/send-message.ts:31` and `src/mcp/broadcast.ts:27` — duplicate `RETRY_DELAYS_S` constant; import `RETRY_DELAYS_S` from `src/mcp/poke-retry.ts` instead.
- CQ-02 `src/mcp/poke-retry.ts:43-50` — `scheduleRetry` creates-then-clears a dummy `setTimeout` to satisfy the non-null `timer` field; change `RetryEntry.timer` to `ReturnType<typeof setTimeout> | null` or reorder init so the dummy is not needed.

**SUGGESTION** (consider): none.

### Verdict

No critical issues. 2 warnings to address (both code-quality tidy-ups, no functional or spec-compliance impact). Ready for `/ts-verify` when warnings are either accepted or fixed.

STATUS: warn
