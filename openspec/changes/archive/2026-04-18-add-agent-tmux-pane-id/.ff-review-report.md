---
status: warn
change: add-agent-tmux-pane-id
generated_at: 2026-04-17T01:40:00Z
stages:
  spec_compliance: { status: pass, issue_count: 0 }
  code_quality:    { status: pass, issue_count: 0 }
  integration:     { status: warn, issue_count: 1 }
issue_count: 1
warn_issues:
  - stage: integration
    id: INT-01
    tag: missing-runtime-assumptions
    file: openspec/changes/add-agent-tmux-pane-id/design.md
    line: 38
    summary: "Design text relies on SQLite `ALTER TABLE ADD COLUMN` default and `PRAGMA table_info` default behavior without a dedicated `## Runtime Assumptions` section documenting the reliance."
---

## Code Review: add-agent-tmux-pane-id

### Summary

| Stage               | Status              |
|---------------------|---------------------|
| Spec Compliance     | PASS / 0 findings   |
| Code Quality        | PASS / 0 findings   |
| Integration Review  | WARN / 1 finding    |

### Stage 1: Spec Compliance

Requirements: 5/5 mapped to implementation (4 MODIFIED + 1 ADDED).
Scenarios: 10/10 covered in code, 10/10 verified in tests.

Mapping:

- `Agents table schema` (MODIFIED)
  - Scenario `Fresh database creates agents table with nine columns` → `src/storage/schema.ts:13-23` + `tests/agents-schema.test.ts:14-29`. The DDL now contains 9 columns; the test asserts column list, `TEXT` type, and `notnull=0` for `tmux_pane_id`. PASS.
  - Scenario `Legacy database auto-migrates by adding tmux_pane_id column` → `src/storage/schema.ts:74-79` (`ensureAgentsHasTmuxPaneId`) + `tests/agents-legacy-migration.test.ts:14-40`. Idempotency verified (second `applySchema` does not throw). PASS.
- `register_agent uses MCP session id as agent_id` (MODIFIED)
  - Scenario `New session registers with tmux_pane_id provided` → `src/mcp/tools.ts:82-109`, `src/mcp/register-agent.ts:4-29`, `src/storage/agents-repo.ts:27-42` + `tests/agents-repo.test.ts:76-82`, `tests/register-agent-tmux-pane.test.ts:17-46`. PASS.
  - Scenario `New session registers without tmux_pane_id` → `tests/agents-repo.test.ts:84-90`. PASS.
- `Repeated register_agent within same session updates metadata` (MODIFIED)
  - Scenario `Same session re-registers with new tmux_pane_id` → COALESCE/UPSERT at `src/storage/agents-repo.ts:33-39` + `tests/agents-repo.test.ts:92-99`. PASS.
  - Scenario `Same session re-registers omitting tmux_pane_id does not clear existing value` → COALESCE preserves prior value + `tests/agents-repo.test.ts:101-108`. PASS.
- `list_agents scoped to caller team` (MODIFIED)
  - Scenario `list_agents returns tmux_pane_id for each agent` → `src/storage/agents-repo.ts:44-50` + `tests/agents-repo.test.ts:110-120` + `tests/register-agent-tmux-pane.test.ts:48-70`. PASS.
- `Tmux pane id persistence` (ADDED)
  - Scenario `Missing tmux_pane_id persists as NULL` → `tests/agents-repo.test.ts:84-90`. PASS.
  - Scenario `Non-tmux environment unaffected by new field` → same test covers (omit case). PASS.
  - Scenario `Opaque string preserved regardless of format` → `tests/agents-repo.test.ts:122-129` asserts literal `'custom-pane-token-xyz'` round-trip. PASS.

### Stage 2: Code Quality

Production files reviewed: 4 (`src/storage/schema.ts`, `src/storage/agents-repo.ts`, `src/mcp/register-agent.ts`, `src/mcp/tools.ts`).
Test files reviewed: 4 (`tests/agents-schema.test.ts`, `tests/agents-legacy-migration.test.ts`, `tests/agents-repo.test.ts`, `tests/register-agent-tmux-pane.test.ts`).

Findings: none at WARN or CRITICAL severity.

Positive observations:

- Migration helper `ensureAgentsHasTmuxPaneId` is named and single-purpose; the comment-free implementation matches project convention (`CLAUDE.md` §00-general: no explanatory comments).
- UPSERT uses `COALESCE(excluded.tmux_pane_id, agents.tmux_pane_id)` — elegant and directly encodes the "omitted means no-change" semantics required by the spec.
- Tests use real `better-sqlite3` DB under `mkdtempSync`; no mocking of storage. Arrange→Act→Assert structure is clean.
- Integration test reaches the real MCP Streamable HTTP transport via `@modelcontextprotocol/sdk`, driving `register_agent` and `list_agents` end-to-end, plus a direct DB cross-check — this is the opposite of the "forwarding-only test" anti-pattern and matches `ts-review` §3c expectations.
- Line width: pre-existing agents-repo.ts already contained >88-col SQL lines and inline type annotations (e.g. line 53, 57 at 112/97 cols before this change). The change adds comparable lines following the same pre-existing style. Not flagged as regression. Project does not enforce 88-col as a hard gate on SQL/type lines in this module.
- No explanatory comments added; intent-only where present (none needed here).
- No hard-coded values; `ONLINE_MS` continues to be the single source of truth for liveness.
- YAGNI: no unused parameters, no speculative abstractions. `tmux_pane_id` is wired only where spec scenarios demand it.
- Verification-kind sanity: Task 4.1 (`manual-verify`) is documentation-only (four markdown files) — no pure logic to extract. Correctly classified.

### Stage 3: Integration Review

Bridge types detected: data-layer only — SQLite column addition + MCP zod input schema extension. No subprocess/RPC/threading/GPU/delegate bridges in this change.

Forwarding-only tests: 0 found. All tests assert on real SQLite row state (`SELECT tmux_pane_id FROM agents ...`) or real MCP tool response payloads.

Observed output blocks reviewed: 10 tasks × {RED + GREEN + REFACTOR} blocks. All RED blocks contain recognizable `AssertionError`/`expected ... to be` failure signals; all GREEN blocks contain `✓` / "Tests N passed" success signals. No evidence fraud.

Runtime Assumptions cross-check: design.md does NOT have a `## Runtime Assumptions` section. Trigger patterns present:

- `design.md:41` — "SQLite 原生支持在 live table 上 `ADD COLUMN`, 对既有行自动 NULL 填充" (reliance on SQLite default behavior for ALTER TABLE)
- `design.md:43` — "`PRAGMA table_info` 幂等检查够用" (reliance on better-sqlite3 pragma behavior)

These are real reliances on external defaults. Both are in fact correct (SQLite has supported `ALTER TABLE ADD COLUMN` since 3.2.0 and adds NULL to existing rows; `PRAGMA table_info` is idempotent and read-only), and the code correctly reflects them. The gap is documentational, not functional.

Finding INT-01 (WARN): design.md contains reliance language without a corresponding `## Runtime Assumptions` section. This is the ts-propose audit gap that `ts-review` §3f is designed to catch. Recommended action: append a short `## Runtime Assumptions` section to `design.md` documenting the two SQLite defaults being relied upon, or note the gap when drafting future similar changes. Does NOT block verify/sync — the code behaves correctly on the reliances.

Integration compatibility:

- daemon-core (`src/daemon/server.ts`, `src/daemon/cleanup.ts`): `applySchema` is still called at boot; cleanup's `SELECT MIN(last_processed_event_id) FROM agents` is unaffected by the new column. PASS.
- mcp-transport (`src/mcp/transport.ts`): input-schema extension in `tools.ts` is handled by existing transport; zod silently strips unknown fields in pre-change clients. PASS.
- events-outbox, mailbox, task-list, contract-registry, contract-subscriptions: none SELECT from `agents` via `*`; all target specific columns (`role`, `agent_id`, `team`). The new column is inert to them. PASS.
- Backward compatibility: fresh DB gets the column via `CREATE TABLE IF NOT EXISTS`; legacy DB gets it via the one-shot idempotent `ALTER TABLE`; existing register_agent callers with no `tmux_pane_id` get NULL; existing list_agents consumers see an additional nullable field they may ignore. PASS.
- Session binding: `register_agent`'s existing `connection_id` collision path (`src/mcp/register-agent.ts:25-26`) is additive and unaffected by the new field. PASS.
- Security: `tmux_pane_id` is persisted as an opaque string via parameterized SQL (`INSERT ... VALUES (?, ...)`), no SQL-injection surface. Downstream interpretation (future `poke` MCP tool, out of scope for this change) will be responsible for sanitizing the value before handing it to `tmux send-keys`. Not a defect of this change.

### Findings by Severity

**CRITICAL** (must fix): none.

**WARNING** (should fix):

- INT-01 — `openspec/changes/add-agent-tmux-pane-id/design.md:38-43`: design text relies on SQLite `ALTER TABLE ADD COLUMN` default and `PRAGMA table_info` default behavior without a dedicated `## Runtime Assumptions` section. Recommendation: append a `## Runtime Assumptions` section enumerating the two SQLite reliances (`ALTER TABLE ADD COLUMN` semantics, `PRAGMA table_info` idempotency) so future reviewers can cross-check them mechanically. Does not block verify or sync.

**SUGGESTION** (consider): none.

### Verdict

No critical issues. 1 warning (documentational gap in design.md; behavior is correct). Run `/ts-verify` when ready.

STATUS: warn
