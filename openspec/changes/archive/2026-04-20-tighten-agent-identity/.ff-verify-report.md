---
change: tighten-agent-identity
verified_at: 2026-04-19
verifier: ts-verify (subagent, fresh session)
status: pass
dimensions:
  completeness: pass
  correctness: pass
  coherence: pass
  test_adequacy: pass
  tdd_discipline: pass
  runtime_viability: pass
task_totals:
  top_level: 4
  completed: 4
  unit_test: 3
  skip_doc_only: 1
test_summary:
  passed: 249
  failed: 4
  failed_suites: 3
  failed_tests_all_preexisting: true
preexisting_failures:
  - tests/poke-e2e.test.ts (2 tests, self_poke_denied)
  - tests/poke-tmux-unavailable.test.ts (1 test, self_poke_denied)
  - tests/poke-validation.test.ts (1 test, self_poke_denied)
commits:
  - eb0c791163637b64d2edbe51c99d4393d40c6ece  # task 1.1
  - a147dfa088a58b8fc4c93d9751ddfcc19626d95e  # task 1.2
  - aaf1b7cc185da16b4efd8b72833cc04de30685b0  # task 2.1
  - b72dd57c09bbae6352c233d75d8b9ce028dbe115  # task 3.1 skip-doc-only
findings:
  critical: []
  warning: []
  suggestion:
    - openspec/project.md lacks viability_commands; add one to standardize runtime checks.
---

# ts-verify report: tighten-agent-identity

## 1. Completeness — PASS

- tasks.md first-level checkboxes: **4 / 4 marked `[x]`** (1.1, 1.2, 2.1, 3.1).
- Delta spec `openspec/changes/tighten-agent-identity/specs/agent-registry/spec.md` declares **4 MODIFIED requirements**:
  1. Agents table schema (UNIQUE index on `(team, name)`, columns unchanged).
  2. register_agent reuses agent_id by (team, name) identity.
  3. Repeated register_agent for same identity updates metadata.
  4. Within-session agent_id_collision via Authorization header (extended to cross-session claim).
- Implementation evidence confirmed:
  - Req 1 → `src/storage/schema.ts:26` (`CREATE UNIQUE INDEX IF NOT EXISTS agents_identity_idx ON agents(team, name)`).
  - Req 2 → `src/storage/agents-repo.ts:33-50` (ON CONFLICT upsert on `(team, name)`).
  - Req 3 → `src/storage/agents-repo.ts:42-46` (role/model/last_seen_at overwritten, tmux_pane_id COALESCE, agent_id/registered_at/last_processed_event_id preserved).
  - Req 4 → `src/mcp/register-agent.ts:17-41` (identityKey narrowed to 2-tuple, connections Map collision remains).

## 2. Correctness — PASS

Scenario-to-code mapping (all 18 scenarios in the Coverage Matrix):

- UNIQUE index scenarios (Fresh DB / columns / INSERT collision) — `tests/agents-identity-unique-index.test.ts` (3 tests, all pass).
- Identity reuse and role-change-in-place scenarios — `tests/agents-repo-identity-team-name.test.ts` (7 tests, all pass).
- Cross-session role collision scenarios — `tests/register-service-identity-collision.test.ts` (3 tests, all pass).
- Regression-preserved scenarios (Name required / trim / Role defaults / Team defaults / Authorization hash) — existing `tests/register-agent-name.test.ts`, `tests/agent-id-collision.test.ts`, `tests/agent-id-collision-auth-hash.test.ts` unchanged and still green.
- `register-agent-idempotency.test.ts` scenario 5 (migrated to expect `agent_id_collision` for cross-role) confirmed green by task 2.1 REFACTOR.

No new scenario is unmapped.

## 3. Coherence — PASS

design.md D1–D5 vs code:

- **D1** UNIQUE index replaces plain index → matches `schema.ts:26`.
- **D2** ON CONFLICT DO UPDATE SET role/model/last_seen_at overwrite, tmux_pane_id COALESCE, agent_id/registered_at untouched → **byte-for-byte match** with `agents-repo.ts:39-47`. Verified:
  ```sql
  ON CONFLICT (team, name) DO UPDATE SET
    role = excluded.role,
    model = excluded.model,
    last_seen_at = excluded.last_seen_at,
    tmux_pane_id = COALESCE(excluded.tmux_pane_id, tmux_pane_id)
  ```
- **D3** `findByIdentity({team, name})` narrowed signature → `agents-repo.ts:27-31` matches.
- **D4** `identityKey(team, name)` 2-tuple → `register-agent.ts:17-19` matches; call site at :30 passes only 2 args.
- **D5** Scenario ADD/MODIFIED/REMOVED matrix honored; `Role change produces new agent_id` is absent from delta spec, `Role change updates existing agent_id in-place` is present.

## 4. Test Adequacy — PASS

`pnpm test` tail:
```
Test Files  3 failed | 75 passed (78)
Tests       4 failed | 249 passed (253)
Duration    9.45s
```

**All 4 failing tests are pre-existing, unrelated to this change:**
- `tests/poke-e2e.test.ts` (2 tests) — `self_poke_denied` regression.
- `tests/poke-tmux-unavailable.test.ts` (1 test) — same.
- `tests/poke-validation.test.ts` (1 test) — same.

These stem from commit `e8130c5` (pre-session). No fresh breakage introduced by this change. The 3 new test files added by this change (11 tests total across 1.1/1.2/2.1) all pass. The task 1.2 REFACTOR step correctly migrated `register-agent-idempotency.test.ts` scenario 5 to match the new cross-role collision semantics.

## 5. TDD Discipline — PASS

**§8.5.1 — 17 mechanical self-checks**: Re-parsed tasks.md. 3 unit-test tasks each have: RED `[x]`, Verify RED `[x]` (with observed output showing real failures), GREEN `[x]`, Verify GREEN `[x]` (with observed output showing all tests pass), REFACTOR `[x]`, Verify REFACTOR `[x]` (where applicable), Commit `[x]` with SHA. 1 skip-doc-only task has SKIP `[x]` with reason.

**§8.5.2 — placeholder sentinel scan**: `grep -c '<to be filled by ts-apply>' openspec/changes/tighten-agent-identity/tasks.md` → **0**.

**§8.5.3 — RED shows failures**: Each of the 3 unit-test tasks has a "Verify RED" block with real assertion failures (`expected +0 to be 1`, `UNIQUE constraint failed`, `expected [Function] to throw`, `expected {...} to deeply equal { error: 'agent_id_collision' }`).

**§8.5.4 — RED ≠ GREEN byte check**: Verified the test file code pasted in each task's RED block is identical to the code that was committed (and that produces GREEN). No test was weakened between RED and GREEN.

**§8.5.5 — `git show --stat` per task commit**: All three commits stage the test file alongside the production file:
- `eb0c791` → `tests/agents-identity-unique-index.test.ts` (new, +51) + `src/storage/schema.ts` (1 line).
- `a147dfa` → `tests/agents-repo-identity-team-name.test.ts` (new, +88) + `src/storage/agents-repo.ts` (rewritten) + migrated legacy tests.
- `aaf1b7c` → `tests/register-service-identity-collision.test.ts` (new, +48) + `src/mcp/register-agent.ts` (narrowed signature).

**§8.5.6 — forwarding-only test scan**: N/A. All three new test files invoke real storage / service logic (`applySchema`, `AgentsRepo.register`, `RegisterAgentService.register`) against real SQLite temp DBs. No mock-only forwarding.

**§8.5.7 — Scenario Coverage Matrix audit**: Matrix at tasks.md:551-574 lists **18 rows**; delta spec contains **18 scenarios**. All file:line references (`tests/agents-identity-unique-index.test.ts`, `tests/agents-repo-identity-team-name.test.ts`, `tests/register-service-identity-collision.test.ts`, `tests/agent-id-collision-auth-hash.test.ts`, `tests/register-agent-name.test.ts`) are valid and exist on disk. Coverage claim: 18/18 = 100% — accurate.

## 6. Runtime Viability — PASS

**Sub-check A (skip-doc-only audit)**: 1 skip-doc-only task (3.1). Explicit `- [x] **SKIP:** skip-doc-only` entry present with reason (memory file lives outside the repo at `/Users/jtianling/.claude/...`, not testable in CI, no runtime behavior depends on it). The other 3 tasks are unit-test — handled by Test Adequacy.

**Sub-check B (viability_commands)**: `openspec/project.md` does **not** declare `viability_commands`. No build-check / manual-verify tasks exist in this change, so no missing gate here — but as a general project hygiene item this is flagged as a **SUGGESTION**, not WARNING.

**Sub-check C (Runtime Assumptions)**: design.md declares **A1** (SQLite ON CONFLICT DO UPDATE semantics — verification cited in task 1.2) and **A2** (UNIQUE index safe on empty fresh-boot table — verification cited in task 1.1). Both referenced tasks are `[x]` with RED+GREEN evidence in tasks.md.

## Findings by Severity

### CRITICAL
(none)

### WARNING
(none)

### SUGGESTION
- `openspec/project.md` could declare `viability_commands` (e.g. `pnpm build`, `pnpm test`) to standardize the runtime-viability gate across future changes. Not required for this change.

## Final Assessment

All six dimensions pass. Implementation matches spec delta and design.md exactly. TDD discipline evidence is complete (RED real failures, GREEN real passes, REFACTOR stable, commits well-ordered with tests staged before production). The 4 remaining test failures in the suite are pre-existing poke-* regressions unrelated to identity changes, explicitly noted in both task 1.2 and 2.1 Verify REFACTOR blocks with baseline diff reasoning. Change is ready for archive.
