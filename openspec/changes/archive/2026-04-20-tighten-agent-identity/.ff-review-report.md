---
status: pass
change: tighten-agent-identity
reviewed_at: 2026-04-19
stages:
  spec_compliance:
    status: pass
    issue_count: 0
  code_quality:
    status: pass
    issue_count: 0
  integration:
    status: pass
    issue_count: 0
issue_count: 0
critical_issues: []
warn_issues: []
suggestions:
  - id: S1
    stage: code_quality
    location: src/mcp/register-agent.ts:48
    text: "`releaseConnection(agent_id, connection_id)` ignores agent_id via `void agent_id`. Pre-existing pattern, not introduced by this change; if the signature can be simplified in a follow-up it would reduce noise."
---

# /ts-review report — tighten-agent-identity

## Stage 1: Spec Compliance

All 18 scenarios in `openspec/changes/tighten-agent-identity/specs/agent-registry/spec.md` map to implementation and tests.

| # | Scenario | Impl file:line | Test file |
|---|---|---|---|
| 1 | Fresh DB creates UNIQUE identity index on (team, name) | `src/storage/schema.ts:26` | `tests/agents-identity-unique-index.test.ts:24` |
| 2 | agents table columns match schema | `src/storage/schema.ts:15-25` | `tests/agents-schema.test.ts` (pre-existing) + `tests/agents-identity-unique-index.test.ts` |
| 3 | Inserting two rows with same (team, name) violates UNIQUE | `src/storage/schema.ts:26` | `tests/agents-identity-unique-index.test.ts:39` |
| 4 | New identity creates a fresh agent_id | `src/storage/agents-repo.ts:33-50` | `tests/agents-repo-identity-team-name.test.ts:22` |
| 5 | Reconnect reuses existing agent_id | `src/storage/agents-repo.ts:39-48` (ON CONFLICT branch) | `tests/agents-repo-identity-team-name.test.ts:32` |
| 6 | Role change updates existing agent_id in-place | `src/storage/agents-repo.ts:42-46` (SET role=excluded.role) | `tests/agents-repo-identity-team-name.test.ts:41, 50` |
| 7 | Reuse updates tmux_pane_id when provided | `src/storage/agents-repo.ts:46` (COALESCE) | `tests/agents-repo-identity-team-name.test.ts:62` |
| 8 | Reuse preserves tmux_pane_id when omitted | `src/storage/agents-repo.ts:46` (COALESCE) | `tests/agents-repo-identity-team-name.test.ts:71` |
| 9 | Team change produces new agent_id | `src/storage/agents-repo.ts:39-48` (team in key, distinct row) | `tests/agents-repo-identity-team-name.test.ts:80` |
| 10 | Name is required and must be non-empty | pre-existing Zod schema | `tests/register-agent-name.test.ts` (pre-existing) |
| 11 | Name after trim must be non-empty | pre-existing Zod schema | `tests/register-agent-name.test.ts` (pre-existing) |
| 12 | Role defaults to "default" when omitted | `src/storage/agents-repo.ts:35` | `tests/register-agent-name.test.ts` (pre-existing) |
| 13 | Team defaults to "default" when omitted | `src/storage/agents-repo.ts:34` | `tests/register-agent-name.test.ts` (pre-existing) |
| 14 | Same session re-registers with new tmux_pane_id | `src/mcp/register-agent.ts:31-33` + repo upsert | `tests/register-service-identity-collision.test.ts:30` (same-sess reuse) + pre-existing |
| 15 | Re-register after reconnect preserves mailbox continuity | `src/storage/agents-repo.ts` (ON CONFLICT does not touch last_processed_event_id) | `tests/agents-repo-identity-team-name.test.ts:50` |
| 16 | Different Authorization credentials on same session id | pre-existing auth-hash binding | `tests/agent-id-collision-auth-hash.test.ts` (pre-existing) |
| 17 | Cross-session same identity under different Authorization reuses agent_id | pre-existing + `src/mcp/register-agent.ts:43-49` (releaseConnection) | `tests/agent-id-collision-auth-hash.test.ts` (pre-existing) |
| 18 | Same (team, name) claimed by different role from live session is collision | `src/mcp/register-agent.ts:30-32` | `tests/register-service-identity-collision.test.ts:22` |

**Removed scenario correctly absent**: The spec's `## REMOVED Requirements` block explicitly removes "Role change produces new agent_id"; no test asserts that behavior, and `tests/agents-repo.test.ts:78-82` was migrated to assert the inverse ("Role change returns same agent_id, role column updated").

**Status**: PASS — 18/18 scenarios covered.

## Stage 2: Code Quality

### `src/storage/schema.ts` (1-line change)
- Line 26: `CREATE UNIQUE INDEX IF NOT EXISTS agents_identity_idx ON agents(team, name)` — minimal, correct, matches D1.

### `src/storage/agents-repo.ts` (full rewrite)
- `findByIdentity({team, name})` — narrow signature matches D3; returns `{ agent_id } | undefined`.
- `register(...)` — single ON CONFLICT UPSERT per D2. No extra round trips, no TOCTOU window.
- `COALESCE(excluded.tmux_pane_id, tmux_pane_id)` correctly preserves existing pane when incoming is NULL.
- `role`, `model`, `last_seen_at` unconditionally overwritten; `agent_id`, `registered_at`, `last_processed_event_id` preserved by omission from SET clause — matches D2.
- Post-upsert `SELECT agent_id` is correct: the INSERT path writes `newId`, the UPSERT path keeps the original id, so `SELECT` returns the authoritative value. No dup row risk (UNIQUE index enforces).
- No mutation, no hidden defaults, function <50 lines.

### `src/mcp/register-agent.ts` (identityKey narrow)
- `identityKey(team, name)` — 2-tuple, matches D4. Uses NUL separator to avoid name-collision with literal sep chars.
- `register(...)` still accepts `role` param and passes it through to repo — spec-compliant (role remains informational).
- Collision branch at line 32 triggers correctly when `(team, name)` bound to different connection_id regardless of incoming role — matches Scenario 18.

### Test quality
- All three new tests use real SQLite via `openDb` + `applySchema`; no mocks, no forwarding assertions. Each test asserts on observable state (DB row content, service return value).
- RED output captured for each file shows genuine failures tied to the pre-change behavior (non-UNIQUE index; 3-tuple signature; cross-role registration succeeding).
- Test names follow `behavior_under_scenario_expected_result` pattern.
- AAA structure present; setup hoisted into `fresh()` helper; cleanup via `afterEach`.
- Verification-kind (unit-test) is sane for all three tasks — pure logic on pure SQLite, no I/O, no subprocess.

### Findings
- No duplication. The delta spec's scenarios 5, 14, 15 each test a different preservation/update axis of the same UPSERT, so the apparent overlap is intentional.
- No YAGNI violations — role is kept because `broadcast_to_role` still uses it (per proposal NG5).
- S1 (suggestion): `releaseConnection` has a `void agent_id` at `src/mcp/register-agent.ts:48` to silence unused-param lint. Pre-existing, not introduced here.

**Status**: PASS — 0 CRITICAL, 0 WARN, 1 SUGGESTION.

## Stage 3: Integration Review

### Bridge types
- SQLite via `better-sqlite3` — synchronous, in-process. No thread boundary, no subprocess.  All three tests exercise the bridge directly (real DB file under `mkdtempSync`).  No bridge-type gaps.

### Forwarding-only tests
- Scanned the three new test files for `toHaveBeenCalledWith`, `vi.fn`, `jest.fn`, `mock(`, `spy` — zero matches.  All assertions hit observable state.  No forwarding-only patterns.

### Observed output blocks
- `grep -c '<to be filled by ts-apply>' openspec/changes/tighten-agent-identity/tasks.md` → **0**.  All ts-apply placeholders replaced.
- RED blocks in tasks.md show concrete failure output (`expected +0 to be 1`, `UNIQUE constraint failed`, `expected { agent_id, team } to deeply equal { error: 'agent_id_collision' }`) distinct from GREEN blocks (`3 passed`, `7 passed`, `3 passed`).  RED ≠ GREEN confirmed.

### Runtime Assumption cross-check
- **A1** (SQLite ON CONFLICT semantics) → Verification points to task 1.2. Task 1.2's test `role change preserves registered_at and last_processed_event_id` (`tests/agents-repo-identity-team-name.test.ts:50-60`) directly verifies the claim: seed `last_processed_event_id=5`, register with new role, assert column unchanged. MATCH.
- **A2** (UNIQUE index on empty table no-op) → Verification points to task 1.1. Task 1.1's tests inspect `PRAGMA index_list/index_info` on a fresh-boot DB and assert no error during `applySchema`; also INSERTs two conflicting rows on the freshly created index to confirm the UNIQUE constraint fires. MATCH.

### Missing runtime-assumption scan
Triggers found in design.md (lines 26, 74, 123, 133, 134, 138, 169). Each mapped:

- L26 `feedback_skip_legacy_db_migration.md 一致` — fresh-boot convention, internal project memo. A2 covers empty-table ingestion. No external-default reliance. OK.
- L74 `agent-registry/spec.md:118 一致` — internal spec cross-reference for tmux_pane_id "omission = no change". Task 1.2 tests both update-when-provided and preserve-when-omitted. Covered.
- L123 `默认值逻辑不变` — role default 'default'. Covered by pre-existing `tests/register-agent-name.test.ts` scenarios 12 and 13. OK.
- L133-134 — meta-statement ("design contains these words but they're internal"). Not an assumption, just audit narrative.
- L138 — A1 itself.
- L169 — migration-plan narrative, not a runtime assumption.

All detected triggers are either covered by A1/A2 or are non-assumption narrative. No missing Runtime Assumption.

**Status**: PASS — 0 issues.

## Summary Table

| Stage | Status | CRITICAL | WARN | SUGGESTION |
|---|---|---|---|---|
| Stage 1 — Spec Compliance | PASS | 0 | 0 | 0 |
| Stage 2 — Code Quality | PASS | 0 | 0 | 1 |
| Stage 3 — Integration | PASS | 0 | 0 | 0 |
| **Total** | **PASS** | **0** | **0** | **1** |

## Verdict

The change is ready to archive. Implementation is minimal (3 production files, 1-line schema change, compact upsert, 2-tuple key), TDD evidence is well-formed (RED ≠ GREEN captured in tasks.md for all three test tasks), all 18 delta scenarios map to real tests or pre-existing regression tests, and both Runtime Assumptions have direct verification in unit tests. Only finding is a pre-existing suggestion unrelated to this change.
