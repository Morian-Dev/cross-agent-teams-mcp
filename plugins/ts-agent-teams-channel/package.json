{
  "name": "ts-agent-teams-channel",
  "version": "0.1.0",
  "description": "Claude Code channel proxy for ts-agent-teams",
  "type": "module",
  "bin": {
    "ts-agent-teams-channel-proxy": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
